// @generated by cmd/mcp-tools-export — DO NOT EDIT. Run `make gen-mcp-docs`.
window.__MCP_TOOLS__ = {
  "core_total": 40,
  "domains": [
    {
      "key": "identity",
      "title": "身份 / 发现 · Identity \u0026 Discovery",
      "tools": [
        {
          "name": "find_org_agent",
          "summary": "Find agents in your organization by name (substring match; empty name lists all).",
          "description": "Find agents in your organization by name (substring match; empty name lists all). Returns [{id, name, assignee_ref}] — pass an entry's assignee_ref straight to assign_task's assignee (it is the ready-to-use \"agent:\u003cid\u003e\" form; do not hand-build it).",
          "params": [
            {
              "name": "name",
              "required": true
            }
          ],
          "tier": "secondary"
        },
        {
          "name": "find_org_channel",
          "summary": "Find channels in your organization by name (substring match; empty name lists all = available channels).",
          "description": "Find channels in your organization by name (substring match; empty name lists all = available channels). Returns [{id, name}]. Use an entry's id directly as post_message's conversation_id (it is a plain channel id — do NOT add a prefix). An empty result means no such channel exists.",
          "params": [
            {
              "name": "name",
              "required": true
            }
          ],
          "tier": "secondary"
        },
        {
          "name": "get_my_profile",
          "summary": "Get the calling agent's own profile: your display_name and agent_ref (the \"agent:\u003cid\u003e\" form others use to @mention you), your organization, the projects you belong to (with role + what you can do in each), and your capabilities.",
          "description": "Get the calling agent's own profile: your display_name and agent_ref (the \"agent:\u003cid\u003e\" form others use to @mention you), your organization, the projects you belong to (with role + what you can do in each), and your capabilities. Call this at the start of a session to learn WHO YOU ARE — your display_name tells you which @mentions are actually for you. Several agents may share a conversation; never assume you are an agent whose name merely appears in a message — only your own display_name from this tool identifies you. In concurrent mode, your resident session is this same Agent's Supervisor control plane and forked executors are this same Agent's isolated execution units; process/workspace/MCP isolation does not make them external agents.",
          "params": [],
          "tier": "core"
        },
        {
          "name": "search_tools",
          "summary": "Find and load DEFERRED tools.",
          "description": "Find and load DEFERRED tools. Your default tool set is the high-frequency core; lower-frequency tools (plans, issues, findings, files, subscriptions, org discovery, node recovery) are loaded on demand. Call search_tools with keywords (e.g. \"plan\", \"issue\", \"file\") and the matching tools become callable immediately; an empty query loads ALL deferred tools. Common deferred read tools: get_issue (read a task's spec from its source issue) via \"issue\", get_plan via \"plan\", download_file (view a file/image someone sent) via \"file\". Discoverability is not absence — if a capability seems missing, search_tools here FIRST before concluding the tool does not exist. Replace semantics: each call loads exactly the tools matching your query (a later call changes the loaded set), so pass every group you need at once. Returns the loaded tool names + summaries.",
          "params": [
            {
              "name": "query",
              "required": false
            }
          ],
          "tier": "core"
        }
      ]
    },
    {
      "key": "work_queue",
      "title": "工作队列 · Work Queue",
      "tools": [
        {
          "name": "claim_task",
          "summary": "Claim an OPEN assignment-pool task (an ownerless pool task_id).",
          "description": "Claim an OPEN assignment-pool task (an ownerless pool task_id). Atomically assigns it to you and starts it (open→running). Only project-member agents may claim; you can hold at most a few claimed pool tasks at once. Returns already_claimed if another agent took it first, or pool_claim_limit_reached if you're at your cap. Once claimed it appears in list_my_tasks.",
          "params": [
            {
              "name": "task_id",
              "required": true
            }
          ],
          "tier": "core"
        },
        {
          "name": "start_task",
          "summary": "Start working on one of your runnable tasks (open→running, sets the execution lease).",
          "description": "Start working on one of your runnable tasks (open→running, sets the execution lease). Pick a task_id from list_my_tasks. You may run up to your concurrency cap at once (1 by default — single-active; more if your profile opts into concurrency); start_task past the cap returns agent_busy until you complete_task, fail_task, or yield a running task. Returns task_not_runnable if the task's dependencies aren't satisfied yet.",
          "params": [
            {
              "name": "task_id",
              "required": true
            }
          ],
          "tier": "core"
        }
      ]
    },
    {
      "key": "tasks_issues",
      "title": "任务 / Issue · Tasks \u0026 Issues",
      "tools": [
        {
          "name": "assign_task",
          "summary": "Assign a task to an identity (e.g.",
          "description": "Assign a task to an identity (e.g. agent:X or user:Y).",
          "params": [
            {
              "name": "assignee",
              "required": true
            },
            {
              "name": "task_id",
              "required": true
            }
          ],
          "tier": "core"
        },
        {
          "name": "close_issue",
          "summary": "Close an issue (sets status=closed).",
          "description": "Close an issue (sets status=closed). Convenience wrapper over update_issue; reopen it later with reopen_issue.",
          "params": [
            {
              "name": "issue_id",
              "required": true
            }
          ],
          "tier": "core"
        },
        {
          "name": "complete_task",
          "summary": "Optionally post a summary and move the task to completed.",
          "description": "Optionally post a summary and move the task to completed. In concurrent mode, call this only after you, the Agent's Supervisor control plane, have judged that this same Agent's executor truly delivered; do not complete merely because an executor exited. If it returns task_non_delivery, read reason_codes and fix/register those exact missing delivery conditions before retrying. For a control-flow DECISION node, pass outcome=\"pass\"/\"reject\" to route its edges. For a REVIEW node, you MUST record your structured verdict via review_verdict=\"pass\"/\"reject\" (+ review_blocking) so the downstream Decision auto-decides — a non-blocking nit is review_verdict=\"pass\", review_blocking=false.",
          "params": [
            {
              "name": "task_id",
              "required": true
            },
            {
              "name": "delivery",
              "required": false
            },
            {
              "name": "outcome",
              "required": false
            },
            {
              "name": "review_blocking",
              "required": false
            },
            {
              "name": "review_reason",
              "required": false
            },
            {
              "name": "review_sha",
              "required": false
            },
            {
              "name": "review_verdict",
              "required": false
            },
            {
              "name": "summary",
              "required": false
            }
          ],
          "tier": "core"
        },
        {
          "name": "create_issue",
          "summary": "Open a new issue in a project you belong to — a discussion/requirement item (NOT an executable task).",
          "description": "Open a new issue in a project you belong to — a discussion/requirement item (NOT an executable task). Provide a title and optional description/tags. Derive executable tasks from it later with create_task's derived_from_issue.",
          "params": [
            {
              "name": "project_id",
              "required": true
            },
            {
              "name": "title",
              "required": true
            },
            {
              "name": "description",
              "required": false
            },
            {
              "name": "tags",
              "required": false
            }
          ],
          "tier": "core"
        },
        {
          "name": "create_task",
          "summary": "Create a task in a project the calling agent belongs to.",
          "description": "Create a task in a project the calling agent belongs to.",
          "params": [
            {
              "name": "project_id",
              "required": true
            },
            {
              "name": "title",
              "required": true
            },
            {
              "name": "assignee",
              "required": false
            },
            {
              "name": "delivery_contract",
              "required": false
            },
            {
              "name": "derived_from_issue",
              "required": false
            },
            {
              "name": "description",
              "required": false
            },
            {
              "name": "dispatch",
              "required": false
            },
            {
              "name": "dispatch_mode",
              "required": false
            }
          ],
          "tier": "core"
        },
        {
          "name": "discard_task",
          "summary": "Terminally DISCARD a non-terminal task (open/running → discarded) — the right way to retire a superseded or mis-created task.",
          "description": "Terminally DISCARD a non-terminal task (open/running → discarded) — the right way to retire a superseded or mis-created task. A reason is required. Unlike complete_task it does not mark the work done (shows Discarded, not Completed); unlike fail_task it does not feed failure/evolution. A terminal task (completed/failed/discarded) is rejected.",
          "params": [
            {
              "name": "reason",
              "required": true
            },
            {
              "name": "task_id",
              "required": true
            }
          ],
          "tier": "core"
        },
        {
          "name": "fail_task",
          "summary": "Terminally mark the task you are running as failed when it cannot progress.",
          "description": "Terminally mark the task you are running as failed when it cannot progress. A detailed reason is required; include what failed, what you tried, and what a next plan generation needs to know. Failed is terminal: do not expect the same task to resume.",
          "params": [
            {
              "name": "reason",
              "required": true
            },
            {
              "name": "task_id",
              "required": true
            }
          ],
          "tier": "core"
        },
        {
          "name": "get_issue",
          "summary": "Get an issue in a project you are a member of (returns title, description, status, tags, created_by, org_ref).",
          "description": "Get an issue in a project you are a member of (returns title, description, status, tags, created_by, org_ref).",
          "params": [
            {
              "name": "issue_id",
              "required": true
            }
          ],
          "tier": "core"
        },
        {
          "name": "get_task",
          "summary": "Get a task the calling agent owns (it must hold a work item for the task).",
          "description": "Get a task the calling agent owns (it must hold a work item for the task).",
          "params": [
            {
              "name": "task_id",
              "required": true
            }
          ],
          "tier": "core"
        },
        {
          "name": "list_issues",
          "summary": "List the issues in a project you are a member of (board overview), optionally filtered by status and/or author.",
          "description": "List the issues in a project you are a member of (board overview), optionally filtered by status and/or author. Requires project_id.",
          "params": [
            {
              "name": "project_id",
              "required": true
            },
            {
              "name": "author",
              "required": false
            },
            {
              "name": "offset",
              "required": false
            },
            {
              "name": "page_size",
              "required": false
            },
            {
              "name": "status",
              "required": false
            }
          ],
          "tier": "core"
        },
        {
          "name": "list_tasks",
          "summary": "List all tasks in a project (board overview), optionally filtered by status and/or assignee.",
          "description": "List all tasks in a project (board overview), optionally filtered by status and/or assignee. Requires project_id; the caller must be a project member.",
          "params": [
            {
              "name": "project_id",
              "required": true
            },
            {
              "name": "assignee",
              "required": false
            },
            {
              "name": "offset",
              "required": false
            },
            {
              "name": "page_size",
              "required": false
            },
            {
              "name": "status",
              "required": false
            }
          ],
          "tier": "core"
        },
        {
          "name": "list_tasks_of_issue",
          "summary": "List the tasks derived from an issue (the reverse of create_task's derived_from_issue link) — see the executable work an issue spawned.",
          "description": "List the tasks derived from an issue (the reverse of create_task's derived_from_issue link) — see the executable work an issue spawned. You must be a member of the issue's project.",
          "params": [
            {
              "name": "issue_id",
              "required": true
            }
          ],
          "tier": "core"
        },
        {
          "name": "reassign_task",
          "summary": "Reassign a task to a different identity.",
          "description": "Reassign a task to a different identity.",
          "params": [
            {
              "name": "assignee",
              "required": true
            },
            {
              "name": "task_id",
              "required": true
            }
          ],
          "tier": "secondary"
        },
        {
          "name": "reopen_issue",
          "summary": "Reopen a closed/resolved issue (sets status=open, the actionable state).",
          "description": "Reopen a closed/resolved issue (sets status=open, the actionable state).",
          "params": [
            {
              "name": "issue_id",
              "required": true
            }
          ],
          "tier": "core"
        },
        {
          "name": "report_manual_recovery_delivery",
          "summary": "Register an already-pushed manual recovery delivery for a task stuck on task_non_delivery after a dead/stale/exhausted executor.",
          "description": "Register an already-pushed manual recovery delivery for a task stuck on task_non_delivery after a dead/stale/exhausted executor. This MCP tool does NOT commit or push; first recover/test/push in the retained worktree, then pass task_id, optional executor_id/worktree, reason, evidence, and git{branch,head_sha,pushed,probed,base_ref,base_known,ahead_of_base,dirty,push_error}. After it returns ok, call complete_task again.",
          "params": [
            {
              "name": "evidence",
              "required": true
            },
            {
              "name": "git",
              "required": true
            },
            {
              "name": "reason",
              "required": true
            },
            {
              "name": "task_id",
              "required": true
            },
            {
              "name": "executor_id",
              "required": false
            },
            {
              "name": "worktree",
              "required": false
            }
          ],
          "tier": "secondary"
        },
        {
          "name": "reset_task",
          "summary": "Tier-3 recovery for a task stranded RUNNING under this Agent's dead executor (its workspace/worktree is gone or its node changed, so it will never make progress): reset it back to the pool (running→open, assignee/lease cleared).",
          "description": "Tier-3 recovery for a task stranded RUNNING under this Agent's dead executor (its workspace/worktree is gone or its node changed, so it will never make progress): reset it back to the pool (running→open, assignee/lease cleared). Reset does NOT fork or re-dispatch an executor: after the task is assigned and work_available wakes its supervisor, that supervisor must explicitly call fork_executor for code/tooling work (or handle supervisor_inline/control work itself). Only use when you've confirmed the executor is truly gone — a task whose lease is still live is rejected (a live agent is nudged, not reset). After repeated resets the center fails the task for evolution triage.",
          "params": [
            {
              "name": "task_id",
              "required": true
            }
          ],
          "tier": "secondary"
        },
        {
          "name": "set_task_issue",
          "summary": "(Re)set or CLEAR a task's derived_from_issue link AFTER creation (previously the create-time link was the only chance to set it).",
          "description": "(Re)set or CLEAR a task's derived_from_issue link AFTER creation (previously the create-time link was the only chance to set it). Pass issue_id to link it (the issue must EXIST and belong to the task's project) or an empty string to clear. Authorized for the task's creator / a project member / its current worker — no work item required. Returns the resulting link.",
          "params": [
            {
              "name": "issue_id",
              "required": true
            },
            {
              "name": "task_id",
              "required": true
            }
          ],
          "tier": "secondary"
        },
        {
          "name": "update_issue",
          "summary": "Edit an issue — set any of title, description, status, tags (omitted fields are left unchanged, applied all-or-none).",
          "description": "Edit an issue — set any of title, description, status, tags (omitted fields are left unchanged, applied all-or-none). status is one of open|in_progress|resolved|closed|discarded|reopened. You must be a member of the issue's project.",
          "params": [
            {
              "name": "issue_id",
              "required": true
            },
            {
              "name": "description",
              "required": false
            },
            {
              "name": "status",
              "required": false
            },
            {
              "name": "tags",
              "required": false
            },
            {
              "name": "title",
              "required": false
            }
          ],
          "tier": "core"
        },
        {
          "name": "update_task",
          "summary": "Update an existing task's card fields: title and/or description, or clear the description.",
          "description": "Update an existing task's card fields: title and/or description, or clear the description. Omitted fields are left unchanged. Description edits on running tasks are rejected with task_description_frozen.",
          "params": [
            {
              "name": "task_id",
              "required": true
            },
            {
              "name": "clear_description",
              "required": false
            },
            {
              "name": "description",
              "required": false
            },
            {
              "name": "title",
              "required": false
            }
          ],
          "tier": "secondary"
        }
      ]
    },
    {
      "key": "plan",
      "title": "Plan / 编排 · Plan \u0026 Orchestration",
      "tools": [
        {
          "name": "add_plan_dependency",
          "summary": "DEPRECATED — prefer edit_plan_topology (add_edge).",
          "description": "DEPRECATED — prefer edit_plan_topology (add_edge). Add an edge to a pending Plan DAG. Both tasks must already be nodes in the Plan; self-edges and cycles are rejected.",
          "params": [
            {
              "name": "from_task_id",
              "required": true
            },
            {
              "name": "plan_id",
              "required": true
            },
            {
              "name": "to_task_id",
              "required": true
            },
            {
              "name": "kind",
              "required": false
            },
            {
              "name": "max_rounds",
              "required": false
            },
            {
              "name": "when",
              "required": false
            }
          ],
          "tier": "secondary"
        },
        {
          "name": "add_task_to_plan",
          "summary": "DEPRECATED — prefer edit_plan_topology.",
          "description": "DEPRECATED — prefer edit_plan_topology. Add an existing backlog task to a pending plan as a node. Once execution starts, completed history is immutable. Optional stage groups the task under a Plan Stage.",
          "params": [
            {
              "name": "plan_id",
              "required": true
            },
            {
              "name": "task_id",
              "required": true
            },
            {
              "name": "stage",
              "required": false
            }
          ],
          "tier": "secondary"
        },
        {
          "name": "archive_plan",
          "summary": "Archive a done or discarded plan without changing that lifecycle outcome.",
          "description": "Archive a done or discarded plan without changing that lifecycle outcome. Returns the terminal plan detail with archived_at.",
          "params": [
            {
              "name": "plan_id",
              "required": true
            }
          ],
          "tier": "secondary"
        },
        {
          "name": "create_plan",
          "summary": "Create a new pending plan in a project you belong to.",
          "description": "Create a new pending plan in a project you belong to. A plan is a DAG of tasks the center auto-dispatches once started. After creating, add tasks with add_task_to_plan, wire dependencies with add_plan_dependency, then start_plan. Optional target_date is RFC3339.",
          "params": [
            {
              "name": "name",
              "required": true
            },
            {
              "name": "project_id",
              "required": true
            },
            {
              "name": "description",
              "required": false
            },
            {
              "name": "target_date",
              "required": false
            }
          ],
          "tier": "secondary"
        },
        {
          "name": "delete_plan",
          "summary": "Hard-delete a never-started pending plan.",
          "description": "Hard-delete a never-started pending plan. Once a plan starts, preserve history and use discard_plan instead. Irreversible.",
          "params": [
            {
              "name": "plan_id",
              "required": true
            }
          ],
          "tier": "secondary"
        },
        {
          "name": "discard_plan",
          "summary": "Permanently abandon a pending, running, or paused plan.",
          "description": "Permanently abandon a pending, running, or paused plan. Non-terminal member tasks become discarded; completed history remains immutable.",
          "params": [
            {
              "name": "plan_id",
              "required": true
            }
          ],
          "tier": "secondary"
        },
        {
          "name": "get_plan",
          "summary": "Read a plan's full detail: its nodes, dependency edges, per-node status, the ready_set, has_failed, and progress{done,total}.",
          "description": "Read a plan's full detail: its nodes, dependency edges, per-node status, the ready_set, has_failed, and progress{done,total}. Scoped to the project you name (a plan in another project is not found).",
          "params": [
            {
              "name": "plan_id",
              "required": true
            },
            {
              "name": "project_id",
              "required": true
            }
          ],
          "tier": "secondary"
        },
        {
          "name": "list_plans",
          "summary": "List a project's plans with a board summary each (status, progress, has_failed, node_count, and a capped nodes preview).",
          "description": "List a project's plans with a board summary each (status, progress, has_failed, node_count, and a capped nodes preview). Use this to find a plan_id to operate on.",
          "params": [
            {
              "name": "project_id",
              "required": true
            },
            {
              "name": "offset",
              "required": false
            },
            {
              "name": "page_size",
              "required": false
            }
          ],
          "tier": "secondary"
        },
        {
          "name": "pause_plan",
          "summary": "Pause new dispatch while preserving the current frontier and immutable execution history.",
          "description": "Pause new dispatch while preserving the current frontier and immutable execution history.",
          "params": [
            {
              "name": "plan_id",
              "required": true
            }
          ],
          "tier": "secondary"
        },
        {
          "name": "remove_plan_dependency",
          "summary": "DEPRECATED — prefer edit_plan_topology (remove_edge).",
          "description": "DEPRECATED — prefer edit_plan_topology (remove_edge). Remove a depends_on edge from a pending Plan DAG. Idempotent when the edge is absent.",
          "params": [
            {
              "name": "from_task_id",
              "required": true
            },
            {
              "name": "plan_id",
              "required": true
            },
            {
              "name": "to_task_id",
              "required": true
            }
          ],
          "tier": "secondary"
        },
        {
          "name": "remove_task_from_plan",
          "summary": "DEPRECATED — prefer edit_plan_topology (remove_node).",
          "description": "DEPRECATED — prefer edit_plan_topology (remove_node). Remove a task node from a pending plan and return it to the backlog. AssignmentPool is independent from Plan.",
          "params": [
            {
              "name": "plan_id",
              "required": true
            },
            {
              "name": "task_id",
              "required": true
            },
            {
              "name": "stage",
              "required": false
            }
          ],
          "tier": "secondary"
        },
        {
          "name": "rerun_failed_node",
          "summary": "Operator recovery for a FAILED/undispatched plan node: clear its dispatch record so the next plan advance re-dispatches it.",
          "description": "Operator recovery for a FAILED/undispatched plan node: clear its dispatch record so the next plan advance re-dispatches it. Pair: use resume_paused_node instead when the node is merely paused (its agent set it aside and went idle).",
          "params": [
            {
              "name": "plan_id",
              "required": true
            },
            {
              "name": "task_id",
              "required": true
            }
          ],
          "tier": "secondary"
        },
        {
          "name": "resume_paused_node",
          "summary": "Operator recovery for a PAUSED plan node (the cross-agent counterpart of resume_task): a node whose agent paused its task and went idle shows `paused` — this resumes it and wakes that agent so it continues.",
          "description": "Operator recovery for a PAUSED plan node (the cross-agent counterpart of resume_task): a node whose agent paused its task and went idle shows `paused` — this resumes it and wakes that agent so it continues. Use rerun_failed_node instead for a failed/undispatched node.",
          "params": [
            {
              "name": "plan_id",
              "required": true
            },
            {
              "name": "task_id",
              "required": true
            }
          ],
          "tier": "secondary"
        },
        {
          "name": "resume_plan",
          "summary": "Resume a paused plan from the same frontier.",
          "description": "Resume a paused plan from the same frontier.",
          "params": [
            {
              "name": "plan_id",
              "required": true
            }
          ],
          "tier": "secondary"
        },
        {
          "name": "start_plan",
          "summary": "Validate and start a pending plan (move it to running), atomically freezing its immutable G0 generation and exposing it through get_plan.active_generation_id.",
          "description": "Validate and start a pending plan (move it to running), atomically freezing its immutable G0 generation and exposing it through get_plan.active_generation_id. The center dispatches each node once its dependencies complete.",
          "params": [
            {
              "name": "plan_id",
              "required": true
            }
          ],
          "tier": "secondary"
        }
      ]
    },
    {
      "key": "conversations",
      "title": "会话 / 消息 · Conversations \u0026 Messaging",
      "tools": [
        {
          "name": "get_my_unread",
          "summary": "List unread messages directed at you — every unread message in your DMs plus every unread @mention of you in channels you're in (excludes channel chatter you weren't mentioned in, and your own messages).",
          "description": "List unread messages directed at you — every unread message in your DMs plus every unread @mention of you in channels you're in (excludes channel chatter you weren't mentioned in, and your own messages). Check this periodically and when you reach a stopping point. Each item carries actor_kind (human|agent|system) + reply_required: a HUMAN directed message you MUST answer (acknowledge + defer, handle now, or decline with a reason) — your reply IS your decision. An AGENT-authored mention is reply-optional (reply_required=false): judge by content — reply if it actually needs one, otherwise just mark_seen to silently acknowledge (no obligation to produce a message). Handle messages at a stopping point — they don't interrupt your current task. After you handle (or silently ack) a message, call mark_seen so it does not come back.",
          "params": [],
          "tier": "core"
        },
        {
          "name": "mark_seen",
          "summary": "Mark a conversation read up to a message id — call this AFTER you reply to (or decide on) a message from get_my_unread, so it is not surfaced again.",
          "description": "Mark a conversation read up to a message id — call this AFTER you reply to (or decide on) a message from get_my_unread, so it is not surfaced again. Pass the conversation_id and the id of the latest message you handled.",
          "params": [
            {
              "name": "conversation_id",
              "required": true
            },
            {
              "name": "message_id",
              "required": true
            }
          ],
          "tier": "core"
        },
        {
          "name": "post_message",
          "summary": "Post a message to a DM/channel, a task, or an issue — ONE tool for all four, selected by target.",
          "description": "Post a message to a DM/channel, a task, or an issue — ONE tool for all four, selected by target. Set target.type to \"conversation\" (a DM or channel, target.id = the conversation_id from the message you were given), \"task\" (target.id = task_id), or \"issue\" (target.id = issue_id). For the main answer to a message you were given, pass reply_to_message_id with that source message id; if replying in a thread also pass parent_message_id. Omit reply_to_message_id for side notifications/handoffs that should not count as answering the source. @mention a participant by name to notify them. Keep your text focused on what you're saying — to share a file, upload it with upload_file and pass the returned file_uri in attachments (the UI renders attachments as preview cards); do not paste raw file URIs into the text.",
          "params": [
            {
              "name": "target",
              "required": true
            },
            {
              "name": "text",
              "required": true
            },
            {
              "name": "attachments",
              "required": false
            },
            {
              "name": "mention_refs",
              "required": false
            },
            {
              "name": "parent_message_id",
              "required": false
            },
            {
              "name": "quoted_message_id",
              "required": false
            },
            {
              "name": "reply_to_message_id",
              "required": false
            }
          ],
          "tier": "core"
        },
        {
          "name": "start_dm",
          "summary": "Start or reuse a same-org 1:1 DM with another agent and send the opening message.",
          "description": "Start or reuse a same-org 1:1 DM with another agent and send the opening message. Use target_agent from find_org_agent's id or assignee_ref. For existing DMs this reuses the conversation; it does not create duplicates.",
          "params": [
            {
              "name": "target_agent",
              "required": true
            },
            {
              "name": "text",
              "required": true
            },
            {
              "name": "reason",
              "required": false
            }
          ],
          "tier": "core"
        },
        {
          "name": "subscribe",
          "summary": "Subscribe an identity (defaults to the calling agent) to a task.",
          "description": "Subscribe an identity (defaults to the calling agent) to a task.",
          "params": [
            {
              "name": "task_id",
              "required": true
            },
            {
              "name": "identity",
              "required": false
            }
          ],
          "tier": "secondary"
        },
        {
          "name": "unsubscribe",
          "summary": "Unsubscribe an identity (defaults to the calling agent) from a task.",
          "description": "Unsubscribe an identity (defaults to the calling agent) from a task.",
          "params": [
            {
              "name": "task_id",
              "required": true
            },
            {
              "name": "identity",
              "required": false
            }
          ],
          "tier": "secondary"
        }
      ]
    },
    {
      "key": "files",
      "title": "文件 · Files",
      "tools": [
        {
          "name": "attach_file",
          "summary": "Attach an existing center file into a scope in the calling agent's own domain.",
          "description": "Attach an existing center file into a scope in the calling agent's own domain.",
          "params": [
            {
              "name": "file_uri",
              "required": true
            },
            {
              "name": "scope",
              "required": true
            },
            {
              "name": "scope_id",
              "required": true
            }
          ],
          "tier": "secondary"
        },
        {
          "name": "download_file",
          "summary": "Download a center file (ac://files/{ulid} or bare ulid) into the agent's workspace.",
          "description": "Download a center file (ac://files/{ulid} or bare ulid) into the agent's workspace.",
          "params": [
            {
              "name": "dest_path",
              "required": true
            },
            {
              "name": "file",
              "required": true
            }
          ],
          "tier": "core"
        },
        {
          "name": "upload_file",
          "summary": "Upload a file from the agent's workspace to the center, optionally placing it in a scope.",
          "description": "Upload a file from the agent's workspace to the center, optionally placing it in a scope.",
          "params": [
            {
              "name": "path",
              "required": true
            },
            {
              "name": "scope",
              "required": false
            },
            {
              "name": "scope_id",
              "required": false
            }
          ],
          "tier": "core"
        }
      ]
    },
    {
      "key": "findings",
      "title": "Findings",
      "tools": [
        {
          "name": "list_findings",
          "summary": "List the shared findings recorded in a plan so far (oldest-first): each has a kind (fact/failure/constraint/patch_summary), the source task, the author, and the gist.",
          "description": "List the shared findings recorded in a plan so far (oldest-first): each has a kind (fact/failure/constraint/patch_summary), the source task, the author, and the gist. Read this before starting plan work to reuse what others already learned.",
          "params": [
            {
              "name": "plan_id",
              "required": true
            }
          ],
          "tier": "secondary"
        },
        {
          "name": "record_finding",
          "summary": "Record a compact FINDING (gist) back to your plan's shared context so sibling and downstream agents can build on it instead of re-discovering it.",
          "description": "Record a compact FINDING (gist) back to your plan's shared context so sibling and downstream agents can build on it instead of re-discovering it. Use it when you verify a fact, hit a dead end (failure — so others skip it), discover a binding constraint, or finish a change worth summarizing (patch_summary). You may only record a finding for a task you are/were assigned. Keep content short.",
          "params": [
            {
              "name": "content",
              "required": true
            },
            {
              "name": "kind",
              "required": true
            },
            {
              "name": "plan_id",
              "required": true
            },
            {
              "name": "task_id",
              "required": true
            }
          ],
          "tier": "secondary"
        }
      ]
    },
    {
      "key": "reminders",
      "title": "提醒 · Reminders",
      "tools": [
        {
          "name": "create_reminder",
          "summary": "Set a reminder that wakes a target agent and delivers a text.",
          "description": "Set a reminder that wakes a target agent and delivers a text. THREE trigger modes: (1) time — schedule once{once_at RFC3339}; (2) recurring — schedule cron{cron_expr, timezone} with optional end_condition (never|until|max_count); (3) EVENT-DRIVEN — pass on_event{entity_type: plan|task|issue, entity_id, event} to arm the reminder when a project entity transitions (plan: completed|failed|stopped; task: completed|failed|reopened|discarded; issue: closed|reopened), then fire ONCE after `delay` (e.g. 5m; default 0) and @-notify `target` (defaults to remindee_agent_id). The watched entity must be in your project. remindee/target must be in your project (owner may cross projects). skip_if_overlap (default true) drops a fire while the previous one is still being handled; deliver_as_creator (default true) delivers as your identity rather than the system identity (a self-reminder always wakes via the system identity).",
          "params": [
            {
              "name": "content",
              "required": true
            },
            {
              "name": "delay",
              "required": false
            },
            {
              "name": "deliver_as_creator",
              "required": false
            },
            {
              "name": "end_condition",
              "required": false
            },
            {
              "name": "on_event",
              "required": false
            },
            {
              "name": "remindee_agent_id",
              "required": false
            },
            {
              "name": "schedule",
              "required": false
            },
            {
              "name": "skip_if_overlap",
              "required": false
            },
            {
              "name": "target",
              "required": false
            }
          ],
          "tier": "core"
        },
        {
          "name": "get_reminder",
          "summary": "Read one reminder by id (you must be its creator, the remindee, or an owner).",
          "description": "Read one reminder by id (you must be its creator, the remindee, or an owner).",
          "params": [
            {
              "name": "reminder_id",
              "required": true
            }
          ],
          "tier": "core"
        },
        {
          "name": "list_reminders",
          "summary": "List reminders you created (default), or by remindee, optionally filtered by status (active|paused|completed|canceled).",
          "description": "List reminders you created (default), or by remindee, optionally filtered by status (active|paused|completed|canceled). Shows next_run_at + fired_count.",
          "params": [
            {
              "name": "creator_ref",
              "required": false
            },
            {
              "name": "remindee_agent_id",
              "required": false
            },
            {
              "name": "statuses",
              "required": false
            }
          ],
          "tier": "core"
        },
        {
          "name": "update_reminder",
          "summary": "Manage a reminder: action=pause | resume | cancel, or edit (pass a new schedule and/or content).",
          "description": "Manage a reminder: action=pause | resume | cancel, or edit (pass a new schedule and/or content). Pausing stops it firing; resume recomputes the next run; cancel is terminal. Only the creator or an owner may update.",
          "params": [
            {
              "name": "action",
              "required": true
            },
            {
              "name": "reminder_id",
              "required": true
            },
            {
              "name": "content",
              "required": false
            },
            {
              "name": "schedule",
              "required": false
            }
          ],
          "tier": "core"
        }
      ]
    },
    {
      "key": "team",
      "title": "团队 · Teams",
      "tools": [
        {
          "name": "add_member",
          "summary": "Add a member to a team under a role the team declared.",
          "description": "Add a member to a team under a role the team declared. member_ref is an identity ref: agent:\u003cid\u003e or user:\u003cid\u003e. An agent belongs to at most one team (exclusivity); a human may join many.",
          "params": [
            {
              "name": "member_ref",
              "required": true
            },
            {
              "name": "role",
              "required": true
            },
            {
              "name": "team_id",
              "required": true
            }
          ],
          "tier": "secondary"
        },
        {
          "name": "assign_roles",
          "summary": "Resolve plan-node roles to concrete agents off a team's current roster (author-time role→agent binding, design §7).",
          "description": "Resolve plan-node roles to concrete agents off a team's current roster (author-time role→agent binding, design §7). Each request names a node_key + role (+ optional avoid_nodes for a Review≠Dev cross-review constraint); strategy is least_busy (default) or round_robin. Returns each node's resolved agent.",
          "params": [
            {
              "name": "requests",
              "required": true
            },
            {
              "name": "team_id",
              "required": true
            },
            {
              "name": "strategy",
              "required": false
            }
          ],
          "tier": "secondary"
        },
        {
          "name": "associate_project",
          "summary": "Associate a project with a team (many-to-many).",
          "description": "Associate a project with a team (many-to-many).",
          "params": [
            {
              "name": "project_id",
              "required": true
            },
            {
              "name": "team_id",
              "required": true
            }
          ],
          "tier": "secondary"
        },
        {
          "name": "create_team",
          "summary": "Create a team in your organization with its template-declared roles.",
          "description": "Create a team in your organization with its template-declared roles. Roles are NOT hardcoded — each role carries its own cli / model / capability_tags / max_concurrency. The team name must be unique within your org.",
          "params": [
            {
              "name": "name",
              "required": true
            },
            {
              "name": "description",
              "required": false
            },
            {
              "name": "roles",
              "required": false
            }
          ],
          "tier": "secondary"
        },
        {
          "name": "create_team_template",
          "summary": "Author + validate a team template: role composition (each role with a count/配比 and its cli/model/capability_tags), portable (team/global-scope) experiences, and Team Memory rules.",
          "description": "Author + validate a team template: role composition (each role with a count/配比 and its cli/model/capability_tags), portable (team/global-scope) experiences, and Team Memory rules. workflow_template_ref is legacy compatibility; prefer rules. Returns the normalized template.",
          "params": [
            {
              "name": "name",
              "required": true
            },
            {
              "name": "roles",
              "required": true
            },
            {
              "name": "description",
              "required": false
            },
            {
              "name": "experiences",
              "required": false
            },
            {
              "name": "rules",
              "required": false
            },
            {
              "name": "workflow_template_ref",
              "required": false
            }
          ],
          "tier": "secondary"
        },
        {
          "name": "delete_team",
          "summary": "Delete a team by team_id (cascades its roles, members and project associations).",
          "description": "Delete a team by team_id (cascades its roles, members and project associations).",
          "params": [
            {
              "name": "team_id",
              "required": true
            }
          ],
          "tier": "secondary"
        },
        {
          "name": "extract_from_team",
          "summary": "Snapshot a LIVE team into a DRAFT team template (design §6): copies its role composition and keeps only the portable (team/global-scope) experiences from the team's center-hosted memory, dropping project-scoped facts.",
          "description": "Snapshot a LIVE team into a DRAFT team template (design §6): copies its role composition and keeps only the portable (team/global-scope) experiences from the team's center-hosted memory, dropping project-scoped facts. Runs a scrub pass that HIGHLIGHTS suspected proprietary tokens (repo/code names, paths, URLs) for you to review — it does NOT auto-remove them. The draft is NOT export-ready: manual curation is still required before you author it as a template.",
          "params": [
            {
              "name": "team_id",
              "required": true
            },
            {
              "name": "counts",
              "required": false
            }
          ],
          "tier": "secondary"
        },
        {
          "name": "get_team",
          "summary": "Read a team by team_id, including its declared roles and per-role config.",
          "description": "Read a team by team_id, including its declared roles and per-role config.",
          "params": [
            {
              "name": "team_id",
              "required": true
            }
          ],
          "tier": "secondary"
        },
        {
          "name": "instantiate_team",
          "summary": "Instantiate a team template into your organization: creates the team + its role composition, builds one REAL agent identity per role*count and binds them as members, provisions the team's center-hosted memory repo and seeds it with the template's portable experiences.",
          "description": "Instantiate a team template into your organization: creates the team + its role composition, builds one REAL agent identity per role*count and binds them as members, provisions the team's center-hosted memory repo and seeds it with the template's portable experiences. The team is PROJECT-INDEPENDENT — to bind it to one or more projects, call associate_project separately. Returns the team, the new agent identities, and a SEPARATE runtime-provisioning plan (enroll each agent) — the template carries no runtime/auth.",
          "params": [
            {
              "name": "template",
              "required": true
            },
            {
              "name": "team_name",
              "required": false
            }
          ],
          "tier": "secondary"
        },
        {
          "name": "list_teams",
          "summary": "List the teams in your organization.",
          "description": "List the teams in your organization.",
          "params": [],
          "tier": "secondary"
        },
        {
          "name": "remove_member",
          "summary": "Remove a member (member_ref) from a team.",
          "description": "Remove a member (member_ref) from a team.",
          "params": [
            {
              "name": "member_ref",
              "required": true
            },
            {
              "name": "team_id",
              "required": true
            }
          ],
          "tier": "secondary"
        },
        {
          "name": "update_team",
          "summary": "Update a team's name and/or description by team_id.",
          "description": "Update a team's name and/or description by team_id. Omit a field to leave it unchanged.",
          "params": [
            {
              "name": "team_id",
              "required": true
            },
            {
              "name": "description",
              "required": false
            },
            {
              "name": "name",
              "required": false
            }
          ],
          "tier": "secondary"
        }
      ]
    },
    {
      "key": "uncategorized",
      "title": "Uncategorized",
      "tools": [
        {
          "name": "complete_plan",
          "summary": "Complete a running or paused plan when its effective nodes are settled.",
          "description": "Complete a running or paused plan when its effective nodes are settled. For an explicit audited override, set force=true and provide a non-empty reason; automatic completion never forces. Idempotent if already done. To add follow-up work to a done plan, use reopen_plan then evolve_plan_generation.",
          "params": [
            {
              "name": "plan_id",
              "required": true
            },
            {
              "name": "force",
              "required": false
            },
            {
              "name": "reason",
              "required": false
            }
          ],
          "tier": "secondary"
        },
        {
          "name": "create_model_catalog_entry",
          "summary": "Add a model to your organization's catalog.",
          "description": "Add a model to your organization's catalog. model_id must be unique in the org; costs and context_window must be \u003e= 0; tier is a free-text capability/fit description.",
          "params": [
            {
              "name": "context_window",
              "required": true
            },
            {
              "name": "display_name",
              "required": true
            },
            {
              "name": "input_cost",
              "required": true
            },
            {
              "name": "model_id",
              "required": true
            },
            {
              "name": "output_cost",
              "required": true
            },
            {
              "name": "tier",
              "required": true
            }
          ],
          "tier": "secondary"
        },
        {
          "name": "create_stage",
          "summary": "Create a Stage in a pending plan.",
          "description": "Create a Stage in a pending plan. Pass releases downstream; reject records evidence and appends an incremental remediation Stage without reopening completed work. Returns stage_id.",
          "params": [
            {
              "name": "acceptance_contract",
              "required": true
            },
            {
              "name": "evaluator_kind",
              "required": true
            },
            {
              "name": "name",
              "required": true
            },
            {
              "name": "plan_id",
              "required": true
            },
            {
              "name": "assignee_ref",
              "required": false
            },
            {
              "name": "depends_on_stages",
              "required": false
            },
            {
              "name": "exhausted_route",
              "required": false
            },
            {
              "name": "max_rounds",
              "required": false
            },
            {
              "name": "pass_route",
              "required": false
            },
            {
              "name": "reject_route",
              "required": false
            },
            {
              "name": "role_ref",
              "required": false
            }
          ],
          "tier": "core"
        },
        {
          "name": "create_template",
          "summary": "LEGACY/DEPRECATED compatibility path for old workflow templates.",
          "description": "LEGACY/DEPRECATED compatibility path for old workflow templates. Prefer authoring Team Memory rules/ through team templates or migration.",
          "params": [
            {
              "name": "content",
              "required": true
            },
            {
              "name": "description",
              "required": true
            },
            {
              "name": "name",
              "required": true
            }
          ],
          "tier": "secondary"
        },
        {
          "name": "curate_team_template",
          "summary": "Mark a team template as CURATED after you have manually reviewed it (design §9: curation is load-bearing).",
          "description": "Mark a team template as CURATED after you have manually reviewed it (design §9: curation is load-bearing). Export refuses an un-curated template, so run this on an extracted/authored draft before export_team_template. Returns the template with curated=true.",
          "params": [
            {
              "name": "template",
              "required": true
            }
          ],
          "tier": "secondary"
        },
        {
          "name": "delete_model_catalog_entry",
          "summary": "Delete a model catalog entry by id.",
          "description": "Delete a model catalog entry by id.",
          "params": [
            {
              "name": "id",
              "required": true
            }
          ],
          "tier": "secondary"
        },
        {
          "name": "delete_template",
          "summary": "LEGACY/DEPRECATED compatibility path for old workflow templates.",
          "description": "LEGACY/DEPRECATED compatibility path for old workflow templates. Prefer deleting or reverting Team Memory rules/ files.",
          "params": [
            {
              "name": "template_id",
              "required": true
            }
          ],
          "tier": "secondary"
        },
        {
          "name": "edit_plan_topology",
          "summary": "Atomically edit a PENDING plan's DAG with a batch of ops.",
          "description": "Atomically edit a PENDING plan's DAG with a batch of ops. Pass base_version (read from get_plan) for optimistic concurrency. ops is an ordered list of {op, ...}: add_node{task_id}, remove_node{task_id}, add_edge{from_task_id,to_task_id,kind?,when?,max_rounds?}, remove_edge{from_task_id,to_task_id}. Only the FINAL shape is validated. Once a plan is running or paused, use evolve_plan_generation instead.",
          "params": [
            {
              "name": "base_version",
              "required": true
            },
            {
              "name": "ops",
              "required": true
            },
            {
              "name": "plan_id",
              "required": true
            }
          ],
          "tier": "secondary"
        },
        {
          "name": "evolve_plan_generation",
          "summary": "Commit an immutable Plan Generation snapshot derived from the active generation.",
          "description": "Commit an immutable Plan Generation snapshot derived from the active generation. Requires parent_generation_id, base_version, idempotency_key, reason, evidence, and a diff with node_decisions plus new tasks/edges. New root tasks must have explicit dependency edges to prior execution unless detached=true; follows_task_id is lineage only and does not create an execution edge. Create new downstream tasks/edges instead of reusing already-dispatched old nodes. Optimistic concurrency rejects stale parent/version; idempotency returns the prior generation for the same key+payload. Running plans switch active generation and dispatch newly-ready nodes in the same transaction; paused plans switch without dispatch. If a plan is already done, call reopen_plan first; it reopens to paused so you can evolve, then resume_plan to dispatch.",
          "params": [
            {
              "name": "base_version",
              "required": true
            },
            {
              "name": "diff",
              "required": true
            },
            {
              "name": "evidence",
              "required": true
            },
            {
              "name": "idempotency_key",
              "required": true
            },
            {
              "name": "parent_generation_id",
              "required": true
            },
            {
              "name": "plan_id",
              "required": true
            },
            {
              "name": "reason",
              "required": true
            }
          ],
          "tier": "secondary"
        },
        {
          "name": "export_team_template",
          "summary": "Export a team template to a shareable JSON document (design §6 import/export path).",
          "description": "Export a team template to a shareable JSON document (design §6 import/export path). ENFORCES the curation gate: an un-curated template is refused (curate it first). The document can be imported into another org — the cross-org sharing mechanism. Set curated=true once the template has passed manual review.",
          "params": [
            {
              "name": "template",
              "required": true
            },
            {
              "name": "curated",
              "required": false
            }
          ],
          "tier": "secondary"
        },
        {
          "name": "fork_executor",
          "summary": "Supervisor-only dispatch control: fork one of your assigned runnable tasks into this same Agent's isolated executor.",
          "description": "Supervisor-only dispatch control: fork one of your assigned runnable tasks into this same Agent's isolated executor. Use after a work_available nudge when the task should run as code/tooling work outside the resident supervisor session. fork_executor performs start/admission for an open task; do NOT call start_task first (an already-running task is tolerated for compatibility). Returns accepted when the fork command is durably enqueued; inspect list_task_executions/get_task_execution for actual executor progress and judge the result before complete_task. Do not use for supervisor_inline/control tasks you should handle in this session.",
          "params": [
            {
              "name": "task_id",
              "required": true
            },
            {
              "name": "context",
              "required": false
            },
            {
              "name": "model",
              "required": false
            }
          ],
          "tier": "core"
        },
        {
          "name": "get_agent_runtime_effective_config",
          "summary": "Read this agent's desired and observed effective non-sensitive runtime configuration.",
          "description": "Read this agent's desired and observed effective non-sensitive runtime configuration. Secrets and environment values are never returned.",
          "params": [],
          "tier": "secondary"
        },
        {
          "name": "get_repo_info",
          "summary": "Get one referenced repo's standard info (omit repo_id for the project's primary repo).",
          "description": "Get one referenced repo's standard info (omit repo_id for the project's primary repo). With live=true, also fetch recent remote commits + branches (no clone). Credentials are NEVER returned. You must be a member of the project.",
          "params": [
            {
              "name": "project_id",
              "required": true
            },
            {
              "name": "live",
              "required": false
            },
            {
              "name": "repo_id",
              "required": false
            }
          ],
          "tier": "core"
        },
        {
          "name": "get_stage",
          "summary": "Read a Stage's derived status (open/running/reopen/done), its member task nodes, and its current bounded-retry round.",
          "description": "Read a Stage's derived status (open/running/reopen/done), its member task nodes, and its current bounded-retry round. Status is projected from the member nodes — it is not a separately-tracked state.",
          "params": [
            {
              "name": "stage_id",
              "required": true
            }
          ],
          "tier": "core"
        },
        {
          "name": "get_task_audit",
          "summary": "Read a project-visible task's stable, paginated lifecycle audit history.",
          "description": "Read a project-visible task's stable, paginated lifecycle audit history. Notes are bounded and secret-shaped content is redacted.",
          "params": [
            {
              "name": "task_id",
              "required": true
            },
            {
              "name": "execution_id",
              "required": false
            },
            {
              "name": "offset",
              "required": false
            },
            {
              "name": "page_size",
              "required": false
            }
          ],
          "tier": "secondary"
        },
        {
          "name": "get_task_execution",
          "summary": "Read one executor run linked to a project-visible task by execution_id, including CLI/model, health_status, recovery_required, branch/SHA/pushed and non_delivery reasons.",
          "description": "Read one executor run linked to a project-visible task by execution_id, including CLI/model, health_status, recovery_required, branch/SHA/pushed and non_delivery reasons. If recovery_required=true, inspect audit/worktree evidence before complete_task.",
          "params": [
            {
              "name": "task_id",
              "required": true
            },
            {
              "name": "execution_id",
              "required": false
            },
            {
              "name": "offset",
              "required": false
            },
            {
              "name": "page_size",
              "required": false
            }
          ],
          "tier": "secondary"
        },
        {
          "name": "get_team_memory_proposal",
          "summary": "Get one Team Memory proposal with current target blob hash and a diff preview.",
          "description": "Get one Team Memory proposal with current target blob hash and a diff preview. Use the returned repo_commit as expected_repo_commit when reviewing.",
          "params": [
            {
              "name": "proposal_id",
              "required": true
            }
          ],
          "tier": "secondary"
        },
        {
          "name": "get_team_rule",
          "summary": "Read one Team Rule body by slug from the exact Team Memory commit returned by get_team_rule_index.",
          "description": "Read one Team Rule body by slug from the exact Team Memory commit returned by get_team_rule_index. Never omit commit. Executor reads must include execution_id; planning sessions may omit it and are audited under the MCP planning session.",
          "params": [
            {
              "name": "commit",
              "required": true
            },
            {
              "name": "slug",
              "required": true
            },
            {
              "name": "execution_id",
              "required": false
            },
            {
              "name": "phase",
              "required": false
            },
            {
              "name": "planning_session_id",
              "required": false
            }
          ],
          "tier": "core"
        },
        {
          "name": "get_team_rule_index",
          "summary": "Read the body-free Team Rule index for the current agent's team and phase (plan, execute, review, recovery).",
          "description": "Read the body-free Team Rule index for the current agent's team and phase (plan, execute, review, recovery). The response includes the Team Memory commit; pass that commit to get_team_rule before acting when a rule description applies.",
          "params": [
            {
              "name": "execution_id",
              "required": false
            },
            {
              "name": "phase",
              "required": false
            }
          ],
          "tier": "core"
        },
        {
          "name": "get_team_rules",
          "summary": "LEGACY/DEPRECATED: read enabled Team Memory rules with full bodies.",
          "description": "LEGACY/DEPRECATED: read enabled Team Memory rules with full bodies. Prefer get_team_rule_index at startup and get_team_rule for audited, commit-bound body reads.",
          "params": [
            {
              "name": "phase",
              "required": false
            }
          ],
          "tier": "core"
        },
        {
          "name": "get_template",
          "summary": "LEGACY/DEPRECATED: get an old workflow template by ID.",
          "description": "LEGACY/DEPRECATED: get an old workflow template by ID. Prefer migrated Team Memory rules/ plus get_team_rules for plan guidance.",
          "params": [
            {
              "name": "template_id",
              "required": true
            }
          ],
          "tier": "secondary"
        },
        {
          "name": "heartbeat",
          "summary": "Renew the execution lease on the task you are currently running so the background lease-checker does not reclaim it (it reclaims a running task whose lease lapses, presuming the agent died).",
          "description": "Renew the execution lease on the task you are currently running so the background lease-checker does not reclaim it (it reclaims a running task whose lease lapses, presuming the agent died). Call it periodically during a long-running task. Lease-only: it does not change status, and is rejected unless the task is running.",
          "params": [
            {
              "name": "task_id",
              "required": true
            }
          ],
          "tier": "core"
        },
        {
          "name": "import_model_catalog",
          "summary": "Bulk-import the model catalog from a JSON array.",
          "description": "Bulk-import the model catalog from a JSON array. mode=upsert (insert-or-update by model_id) or replace (swap the whole org catalog). Any invalid entry (bad schema, duplicate model_id, negative cost) rejects the WHOLE batch — nothing is half-applied.",
          "params": [
            {
              "name": "json",
              "required": true
            },
            {
              "name": "mode",
              "required": true
            }
          ],
          "tier": "secondary"
        },
        {
          "name": "import_team_template",
          "summary": "Import a team template from an exported JSON document (design §6): re-homes it into YOUR organization as a fresh, un-curated template.",
          "description": "Import a team template from an exported JSON document (design §6): re-homes it into YOUR organization as a fresh, un-curated template. This is how a template shared from another org lands here. Re-review + curate before you re-export it.",
          "params": [
            {
              "name": "document",
              "required": true
            }
          ],
          "tier": "secondary"
        },
        {
          "name": "list_files",
          "summary": "List files and images attached to a task, issue, or conversation scope in the calling agent's own domain.",
          "description": "List files and images attached to a task, issue, or conversation scope in the calling agent's own domain. Use the returned URI with download_file.",
          "params": [
            {
              "name": "scope",
              "required": true
            },
            {
              "name": "scope_id",
              "required": true
            }
          ],
          "tier": "core"
        },
        {
          "name": "list_messages",
          "summary": "Read the chat history of ONE conversation you participate in — a DM, a channel, or a task/issue/plan conversation.",
          "description": "Read the chat history of ONE conversation you participate in — a DM, a channel, or a task/issue/plan conversation. This is how you catch up on context: get_my_unread only shows messages addressed to you (your DMs + @mentions), while list_messages returns the FULL message stream of a conversation, including messages you were never mentioned in or already marked seen. Pass conversation_id (from get_my_unread, find_org_channel, or a message you were given). Returns the most recent messages (limit, default 50, max 50) oldest→newest, plus has_more and next_before_message_id; to read OLDER history, call again with before_message_id = next_before_message_id instead of requesting a huge page. You must be an active participant — a channel you have not joined returns not_a_channel_member.",
          "params": [
            {
              "name": "conversation_id",
              "required": true
            },
            {
              "name": "before_message_id",
              "required": false
            },
            {
              "name": "limit",
              "required": false
            }
          ],
          "tier": "core"
        },
        {
          "name": "list_model_catalog_entry",
          "summary": "List your organization's model catalog — the user-managed set of models agents may run (each with model_id, display_name, per-MTok input/output cost, context_window, and a free-text tier/capability description).",
          "description": "List your organization's model catalog — the user-managed set of models agents may run (each with model_id, display_name, per-MTok input/output cost, context_window, and a free-text tier/capability description).",
          "params": [],
          "tier": "secondary"
        },
        {
          "name": "list_my_tasks",
          "summary": "Your single \"what do I have to do?\" query.",
          "description": "Your single \"what do I have to do?\" query. Returns the open/running tasks assigned to you that are runnable now (their dependencies are satisfied) — each with task_id, title, status, lease_expires_at, and any legacy blocked_reason fields. start_task one (by task_id) to begin it. Call it at the start of your loop and after finishing a task.",
          "params": [],
          "tier": "core"
        },
        {
          "name": "list_project_repos",
          "summary": "List the code repositories a project references (label, description, url, provider, default_branch, is_primary).",
          "description": "List the code repositories a project references (label, description, url, provider, default_branch, is_primary). The standard repo-info surface — credentials are NEVER returned. You must be a member of the project.",
          "params": [
            {
              "name": "project_id",
              "required": true
            }
          ],
          "tier": "core"
        },
        {
          "name": "list_task_executions",
          "summary": "List stable executor runs linked to a project-visible task, reconstructed from persisted executor lifecycle events.",
          "description": "List stable executor runs linked to a project-visible task, reconstructed from persisted executor lifecycle events. Returns health_status, recovery_required, last_effective_activity_at, branch/SHA/pushed and non_delivery reasons.",
          "params": [
            {
              "name": "task_id",
              "required": true
            },
            {
              "name": "execution_id",
              "required": false
            },
            {
              "name": "offset",
              "required": false
            },
            {
              "name": "page_size",
              "required": false
            }
          ],
          "tier": "secondary"
        },
        {
          "name": "list_team_memory_proposals",
          "summary": "List Team Memory proposals for your current team.",
          "description": "List Team Memory proposals for your current team. Defaults to pending. Proposals are audit workflow records and are not loaded by get_team_rules.",
          "params": [
            {
              "name": "kind",
              "required": false
            },
            {
              "name": "limit",
              "required": false
            },
            {
              "name": "offset",
              "required": false
            },
            {
              "name": "status",
              "required": false
            },
            {
              "name": "statuses",
              "required": false
            }
          ],
          "tier": "secondary"
        },
        {
          "name": "list_templates",
          "summary": "LEGACY/DEPRECATED: list old org workflow templates.",
          "description": "LEGACY/DEPRECATED: list old org workflow templates. New plan/runtime guidance should live in Team Memory rules/ and be read with get_team_rules.",
          "params": [],
          "tier": "secondary"
        },
        {
          "name": "propose_team_memory_change",
          "summary": "Propose an add/update/disable/delete change to your current Team Memory.",
          "description": "Propose an add/update/disable/delete change to your current Team Memory. The team is resolved from your membership; do not pass team_id. A proposal does not affect get_team_rules or runtime until promoted. Promotion affects only new planning generations and new executor forks; in-flight snapshots keep their old commit.",
          "params": [
            {
              "name": "idempotency_key",
              "required": true
            },
            {
              "name": "operation",
              "required": true
            },
            {
              "name": "rationale",
              "required": true
            },
            {
              "name": "target_kind",
              "required": true
            },
            {
              "name": "candidate",
              "required": false
            },
            {
              "name": "evidence_refs",
              "required": false
            },
            {
              "name": "target",
              "required": false
            }
          ],
          "tier": "secondary"
        },
        {
          "name": "reopen_plan",
          "summary": "Reopen a done plan to paused so you can append a follow-up immutable generation with evolve_plan_generation.",
          "description": "Reopen a done plan to paused so you can append a follow-up immutable generation with evolve_plan_generation. Completed task history stays immutable; after evolving, call resume_plan to dispatch newly-ready nodes. Archived or discarded plans cannot be reopened.",
          "params": [
            {
              "name": "plan_id",
              "required": true
            }
          ],
          "tier": "secondary"
        },
        {
          "name": "review_team_memory_proposal",
          "summary": "Promote or reject a pending Team Memory proposal.",
          "description": "Promote or reject a pending Team Memory proposal. Only a Team policy Curator agent may use this MCP review path. You must pass expected_repo_commit, expected_proposal_status=pending, and a review comment; warning codes must be acknowledged before promotion. Promotion affects only new planning generations and new executor forks.",
          "params": [
            {
              "name": "action",
              "required": true
            },
            {
              "name": "comment",
              "required": true
            },
            {
              "name": "expected_repo_commit",
              "required": true
            },
            {
              "name": "proposal_id",
              "required": true
            },
            {
              "name": "acknowledge_warnings",
              "required": false
            },
            {
              "name": "expected_proposal_status",
              "required": false
            }
          ],
          "tier": "secondary"
        },
        {
          "name": "runtime_deploy_restart",
          "summary": "Deploy a pushed AgentCenter commit through this worker's authenticated control stream and wait for restart status.",
          "description": "Deploy a pushed AgentCenter commit through this worker's authenticated control stream and wait for restart status. The server resolves target_ref from repo_url and verifies it equals target_sha, then verifies base_ref is an ancestor; caller-supplied verification claims are ignored.",
          "params": [
            {
              "name": "repo_url",
              "required": true
            },
            {
              "name": "target_ref",
              "required": true
            },
            {
              "name": "target_sha",
              "required": true
            },
            {
              "name": "base_ref",
              "required": false
            },
            {
              "name": "mode",
              "required": false
            },
            {
              "name": "prefix",
              "required": false
            },
            {
              "name": "timeout_ms",
              "required": false
            }
          ],
          "tier": "secondary"
        },
        {
          "name": "update_model_catalog_entry",
          "summary": "Update a model catalog entry by id (model_id, display_name, costs, context_window, tier).",
          "description": "Update a model catalog entry by id (model_id, display_name, costs, context_window, tier).",
          "params": [
            {
              "name": "context_window",
              "required": true
            },
            {
              "name": "display_name",
              "required": true
            },
            {
              "name": "id",
              "required": true
            },
            {
              "name": "input_cost",
              "required": true
            },
            {
              "name": "model_id",
              "required": true
            },
            {
              "name": "output_cost",
              "required": true
            },
            {
              "name": "tier",
              "required": true
            }
          ],
          "tier": "secondary"
        },
        {
          "name": "update_template",
          "summary": "LEGACY/DEPRECATED compatibility path for old workflow templates.",
          "description": "LEGACY/DEPRECATED compatibility path for old workflow templates. Prefer updating Team Memory rules/.",
          "params": [
            {
              "name": "content",
              "required": true
            },
            {
              "name": "description",
              "required": true
            },
            {
              "name": "name",
              "required": true
            },
            {
              "name": "template_id",
              "required": true
            }
          ],
          "tier": "secondary"
        }
      ]
    }
  ],
  "note": "GENERATED by `make gen-mcp-docs` (cmd/mcp-tools-export) from internal/mcphost — do not edit by hand.",
  "secondary_total": 65,
  "total": 105
};
