# v2.7 验收计划单（Acceptance Plan）

| Field | Value |
|---|---|
| Owner | @AgentCenterTester（验收执行 owner：测试方案 + 集成/e2e 实现+执行 + 最终验收）；@AgentCenterPD 设计意图复审 + 口径仲裁 |
| Status | Draft / Living（随 D/E 落地持续细化；E2/#106 执行） |
| 验收时机 | 各阶段里程碑逐块复验（已在做）；E2 做完整端到端走查；任一项不过 = ship-block |
| 关联 | [v2.7-domain-refactor-plan.md](v2.7-domain-refactor-plan.md) / [ADR-0046..0052](../design/decisions/) / 11 条 OQ（plan §10） |

> **格式**：分域验收（A0/B/C/D/E）+ 跨 BC 端到端场景 + OQ 不变量逐条核 + 旧栈退役核对 + ship checklist。
> **模式**：延续 v2.4-D/v2.5/v2.6 的 verifier-亲验纪律——每块逐条勾选，全过 + 端到端走通 → **验收 owner（@AgentCenterTester）证据齐全给 "ship-ready"** + @AgentCenterPD 设计复审通过 → @oopslink 拍 ship → tag。
> **前提**：v2.7 全新部署、不迁移旧数据；测试机 oopslink@home（darwin arm64）；fresh install 空库起。

---

## § -1. Ship-blocking 验收协议（最高优先级 / 不可跳）

> **教训（v2.6）**：v2.6 有验收计划、也"验收"了，但 build 仍挂（AppLayout build-break → v2.6.1 补）。根因：① 信 dev 的 "gates green" 报告、没亲自在干净环境复跑；② 没跑**生产构建**（dev 模式能过、`tsc -b`/`vite build` 会挂）；③ 没在 clean checkout 跑（增量缓存掩盖）；④ 没跑**真实发布产物**端到端。
>
> **v2.7 铁律**：以下每条由 **@AgentCenterTester（验收 owner）亲自执行**，**不信报告**，**贴出证据**，任一不过 = ship-block，**没有这些就没有 "ship-ready" 信号**（纪律本身不变，仅执行者由 PM 改为验收 owner；@AgentCenterPD 保留设计复审 + 口径仲裁）：

1. **干净环境**：fresh clone / clean build（`git clean -xfd` 或全新 checkout），杜绝增量缓存掩盖。
2. **验收 owner 亲自跑全门禁并贴输出**：`go build ./...`、`go test ./internal/... ./tests/integration/...`、`go test ./tests/e2e/... -count=1`、`make lint`、**`tsc -b --force`（生产类型检查）**、**前端生产构建 `vite build`（不是 dev server）**、`vitest`。
3. **跑真实发布产物**：`make release` → 全新 install → `start` → 在 mac 上走 §2 端到端主流程（不是 dev server / 不是源码态）。
4. **证据**：贴门禁命令输出 + 端到端走查证据（关键步骤命令输出/截图）。
5. ship-ready 信号 = 上述全部亲手通过 + 证据齐 → 才发；**绝不基于"应该没问题"**。

---

## § 0. 门禁（每块 + 最终都必过）

```
go build ./...
go test ./internal/... ./tests/integration/...
go test ./tests/e2e/... -count=1
make lint            # go vet + arch lint + SPA tsc -b + raw-color grep
（前端）tsc -b --force + vitest
```

---

## § 1. 分域验收

### A0 — Conversation / File URI 基础
- [ ] Conversation.kind = `channel | issue | task | dm`（无 project_channel）；channel owner_ref=`id://organizations/{id}`、issue/task=`pm://...`、dm 无。
- [ ] Message.context_refs 带 work_item_ref/task_ref/agent_ref；UI 可按 work_item_ref 分段、不破。
- [ ] 统一 MessageAttachment（uri ac://files/{ulid}+filename+mime+size，图/文件不分）。
- [ ] FileURI 值对象 + resolver（hash(ulid) 分桶）；FileReference({scope,scope_id}→uri)；BlobStore 接缝（ULID 身份 + content sha256 元数据）。
- [ ] outbox 表 + 幂等投影器骨架（dedup by event_id）。

### B — ProjectManager 域
- [ ] 无全局/跨 Project Task·Issue（拒空 ProjectID）。
- [ ] Issue/Task 状态机强制（§2.2）；blocked 必带 reason；completed 由 assignee/agent；**verified 非本任务完成者（含另一 Agent，ErrSelfVerify）**。
- [ ] 嵌套端点 `/api/projects/{id}/{issues,tasks(+子动作),members,code-repos}`，requireProjectMember 门控；跨 Project→403/404。
- [ ] CreateTask/AssignTask 经 **outbox projector** 建会话 + 同步 participant（不内联）；reassign→旧 WorkItem superseded+新建。
- [ ] **OQ13**：unassign/reassign/reopen 出局 assignee **保留为 sticky 订阅者**，仅显式 Unsubscribe 移除；creator 不可退。
- [ ] CreateProject **不**自动建 channel；channel org 级、显式成员、可选 project_ref 软标签（无约束）。
- [ ] 前端：Issues/Tasks 内嵌 ProjectDetail tab，无全局页、无跨项目聚合。

### C — Agent BC
- [ ] AgentRun 不存在于 API；Agent 长驻模型。
- [ ] CreateAgent 必选 Worker、worker_id 不可变；Start/Stop/Restart/Reset(scope+二次确认)。
- [ ] **StopAgent 不置 WorkItem blocked**；**OQ11**：AgentWorkItem 无 blocked 态（Task blocked→取消 WorkItem；unblock→新建）。
- [ ] availability 派生 `available|busy|unavailable`，Worker 离线优先级最高。
- [ ] AgentWorkItem 多 AgentInteraction（wait→wake）；AgentActivityEvent 流；消息按 work_item_ref 分段。
- [ ] 前端：Agent list/建(选 Worker)/生命周期控制/WorkItem 队列/活动流/availability 徽章。

### D — Environment 域
- [ ] **D1**：一 Worker 控多 Agent（per-worker 独立 offset）；**重连按 offset 续传不重复破坏命令**（日志层：已 ack 不重投 + 命令带 idempotency_key）；Worker 掉线→Agent unavailable；worker-initiated + 复用 TLS/auth。
- [ ] **D2**：MCPHost 暴露 OQ4 工具集、**per-agent 域隔离**（own Project/Org、读限 own WorkItems、no self-verify、管理面 human-only）；两通道（stdin/stdout 驱动→AgentActivityEvent，**stdout 不进 Conversation** / MCP 反向操作）；显式人可见通信（post_task_message 唯一文本入会话路径；request_input/block_task/complete_task 携带文本）；唤醒闭环（OQ5：系统不判语义、Agent 自决、人+他 Agent 输入、忙缓存、同到合并）；**执行 cutover**（旧 taskruntime→新 AgentController）。
- [ ] **D5(#108)**：SSE stream-first 推送（同命令日志、断线回落 poll、保证同 poll 路径）。
- [ ] **D3**：FileTransferSession 上传/下载（create 返回 ac://files/{ulid}+transfer_uri、完成后可读）+ 可达性授权 + 引用计数 GC（7 天宽限，含孤儿）。
- [ ] **D2-g**：ClaudeCode 契约测试（stub claude）：start→注入→输出→request_input→调 MCP→stop/restart。
- [ ] **🔴 D2-f cutover gate — 两通道都过真实 claude（必须前置于 cutover 翻开关）**。**可执行用例矩阵 + round-trip 证据格式 + go/no-go 判定见 [§ A](#-a-d2-f--d2-g-真-claude-可执行验收cutover-gate-细化)**。stub-claude 喂自编格式、会假性通过；真 claude gate 在 D2-c-ii-C 已抓到两处静默失效（argv 删了 `--print`；解析器对不上 claude 2.1.156 嵌套格式→通道②全 Unknown）。翻 cutover 前必须用**真实 claude** 验证：
  - **通道①（注入）**：start→注入 brief→claude 收到（✅ 已过，输入编码正确，claude 2.1.156）；唤醒回复注入同理。
  - **通道② 输出解析**：真 claude stdout(嵌套 assistant/user message.content[] + result/system) → 新 D2 解析器产出正确 activity 事件（不再 Unknown）（D2-c-ii-D）。
  - **通道② MCP 工具调用**：真 claude load mcp-config + 调一个工具打到真 admin → 正确副作用（D2-g）。
  - 正确 argv = `--print --input-format stream-json --output-format stream-json --verbose`（保留 --print、只去位置 prompt）。
  - **绝不在"从没跑过真实 claude"或"任一通道未过"的状态翻 cutover**（否则 agent 在跑但输出全丢/一个动作都做不了=静默失效）。
- [ ] **D2-g MCP 工具契约（per-tool 端到端，必须真实 admin 非 fake）**：16 个 OQ4/文件工具逐个 MCP call→真实 admin 端点→断言成功/正确副作用。**逐工具用例矩阵见 [§ A.4](#-a4-通道-mcp-工具契约用例矩阵d2-g--16-工具逐个真实-admin-非-fake)**。**教训**：recording-fake 单测只证"转发了选定字段名"、证不了"字段名匹配 admin 契约"（b3 的 post_task_message `text`vs`content` bug 就是这盲区）——必须端到端机械验。

### E — 整合 / UI / Release
- [ ] Project/Agent/Environment 页 + Conversation UI（task/issue owner banner / work_item 分段 / 附件按元数据展示）。
- [ ] **E1 补**：订阅 subscribe/unsubscribe 前端 UI（D2 起仅后端端点）；项目成员管理 UI；**derive-from-message（OQ12）**（多选消息→建 Task/Issue→forwarded 卡片入会话，零拷贝附件）。
- [ ] **E2**：本验收单全过 + 文档(README/CHANGELOG/迁移说明) + release notes。

---

## § 2. 跨 BC 端到端场景（mac 实操走查）

- [ ] **主流程**：建 Project → 建 Task → 指派 Agent → Agent 要输入(request_input) → 人回复 → Agent 续(新 AgentInteraction) → complete → creator/另一 Agent verify。
- [ ] **停 Agent**：active 工作中 StopAgent → 不误置 blocked，工作显示 paused（availability unavailable）。
- [ ] **重派分段**：Task 重派 A→B → 同一 Task 会话内按 work_item_ref 出现新段；A 仍在会话（sticky 订阅，OQ13）。
- [ ] **文件**：上传附件 → 拿 ac://files URI → Agent 经 URI 引用；删消息/任务 → 软删引用、blob 不立删、过宽限期 GC。
- [ ] **唤醒**：waiting_input 时人回复唤醒；忙时缓存；同到合并；他 Agent（编排者）也能回。
- [ ] **域隔离**：Agent 跨 Project/Org 操作被拒；Agent 不能验自己完成的 Task；管理面（建 Project/Member/Agent/Worker）Agent 不可达。
- [ ] **重连不重复破坏**：worker 断线重连，stop/reset 不二次执行（日志层 + worker 处理器 idempotency_key 双层）。

---

## § 3. OQ 不变量逐条核（11 条）

- [ ] OQ1 outbox 三通道分治（DB outbox / Worker 控制流 / 观测流），projector 同 tx MarkApplied 幂等。
- [ ] OQ2 availability 3 态、Worker 优先级最高、不进不变量。
- [ ] OQ3 stream 优先+poll 兜底（同日志）、offset 累积 ack、idempotency_key。
- [ ] OQ4 MCP 工具集 + per-agent 域隔离。
- [ ] OQ5 唤醒：系统不判语义、Agent 自决。
- [ ] OQ6 ProjectMember=投递+最小写闸、域隔离非角色权限、Org member 是 Project member 前提。
- [ ] OQ7 Agent cwd={home}/workspace。
- [ ] OQ8 文件 ULID 身份 / hash(ulid) 分桶 / 引用层 / 可达性授权 / sha256 元数据 / 无自动去重。
- [ ] OQ9 软删引用 + 异步引用计数 GC + 7 天宽限。
- [ ] OQ11 AgentWorkItem 无 blocked 态。
- [ ] OQ12 derive-from-message 多选转发。
- [ ] OQ13 出局 assignee sticky 保留。

---

## § 4. 旧执行栈退役核对（#107，D 之后、ship 前）

- [ ] 旧 discussion BC + 旧 task 模型层 + derive 后端逻辑删除。
- [ ] drop 旧 `projects`/`issues`/`tasks` 表（迁移号 #107 执行时分配，非 0044）。
- [ ] CLI 旧模型退役。
- [ ] 无两套 Issue/Task/Agent 模型并存；无未路由死 handler 残留。
- [ ] 执行已切到新 Worker/AgentController（旧 taskruntime 不再被引用）。

---

## § 4b. 已知 v1 限制（接受 + hardening follow-up）

- **MCPHost worker-token 注入**：agent 的 MCP server 子进程经 env 拿 **worker bearer**（权力大于单 agent，可操作该 worker 上任意 agent）；claude 模型运行时可见该 token → 被污染的 agent 理论上能冒充同 worker 上其它 agent。**v1 接受**（单用户、agents 均属用户、同信任域）。**Hardening（多用户/不可信 agent 前必做）**：注入 **per-agent 限域 token** 而非 worker bearer。决定 2026-05-30（@oopslink #1=B 后）。

## § 5. Ship Checklist

- [ ] § 0 门禁全绿（含 e2e）。
- [ ] § 1 分域 + § 2 端到端 + § 3 OQ + § 4 退役 全过。
- [ ] 文档：README / CHANGELOG / v2.6→v2.7 迁移说明（drop+recreate、数据丢失提示）。
- [ ] release notes。
- [ ] **@AgentCenterTester（验收 owner）证据齐全给 "ship-ready"** + @AgentCenterPD 设计复审通过 → @oopslink 拍 ship → tag v2.7.0。

> 本单为 living draft：A0/B/C/D1 已逐块复验通过；D2/D3/D5/E 落地时补充细化各条具体步骤/期望/failure mode。

---

## § A. D2-f / D2-g 真 claude 可执行验收（cutover gate 细化）

> **归属**：@AgentCenterTester 维护（2026-05-30 起）。本节把 §1-D「🔴 D2-f 两通道 cutover gate」+「D2-g per-tool 端到端」细化成**可复现用例矩阵 + 强制 round-trip 证据格式**。标准/门沿用 §-1（真实产物）+ §1-D（两通道真 claude），不另起门。
> **状态**：用例已备好（dev 在 D2-e，f/g 未到，先备后用）。**实现锚点已由 @AgentCenterDev 回填（2026-05-30）**：
> - **解析器**：`internal/workerdaemon/claude_stream.go`（package `workerdaemon`），`ParseStreamLine(line []byte) ([]StreamEvent, error)` → `[]StreamEvent`。
> - **AgentActivityEvent.event_type = `StreamEvent.Type`**（controller 原样上报），取值枚举：`system | assistant_text | thinking | tool_use | tool_result | result | rate_limit | unknown`。（领域层 `internal/agent/activity_event.go` 的 event_type 是自由字符串，由这组填充。）
> - **真 claude fixture（已提交）**：`internal/workerdaemon/testdata/claude_2.1.156_stream.jsonl`（真 2.1.156 的 system/assistant/result 行）。⏳ 缺 `tool_use`+`tool_result` 多轮 fixture（已请 dev 补录，见 §A.3 C2-tooluse/C2-toolresult）。
> - **16 工具端点见 §A.4 代码锚点列**（已回填）。
> - ⏳ 通道② MCP 闭环（claude load mcp-config 调工具→admin）dev 尚未做，是 D2-f 前置 gate；真 claude e2e 归 tester，dev 到 D2-f 同步 mcp-config 生成 + 启动细节。

### § A.0 验收前置（每次执行都先固定并贴出）

- **真 claude 版本固定**：`claude --version` 必须贴出，基线 **claude 2.1.156**；版本漂移即重验（CLI 漂移是本 gate 存在的根因）。
- **环境**：测试机 oopslink@home（darwin arm64）；fresh install 空库；FLAG validation 模式（cutover 翻开关**前**的 additive 验证，不碰现网执行路径）。
- **argv 回归断言（GATE-5，每次跑前 assert）**：实际拉起的 `claude` argv =
  `claude --print --input-format stream-json --output-format stream-json --verbose --session-id <agentID> [--mcp-config <path>]`（dev 确认 2026-05-30；已过真 claude 2.1.156）
  - ✅ 必含 `--print`（D2-c-ii-C 抓到的回归：曾被错删 → 进程行为变化）。
  - ✅ 必含 `--input-format stream-json`（长驻注入通道①）。
  - ✅ `--session-id` = **agentID**（长驻会话身份）。
  - ✅ 用 stdin 注入 user message，**无位置参数 prompt**、**无 `-p`**（旧 adapter 用 `-p` 是一次性模式，cutover 后是长驻）。
- **stdin 注入格式（dev 确认，已过真 claude）**：每行一条
  `{"type":"user","message":{"role":"user","content":[{"type":"text","text":"..."}]}}`
- 旧 `internal/agentadapter/claudecode/adapter.go` 是 taskruntime 期一次性 adapter（扁平 stream-json 解析、`-p` 一次性），**不是** cutover 后的 AgentController 路径；本 gate 验的是新 D2 路径（`internal/workerdaemon/claude_stream.go`），二者不可混用。

### § A.1 Round-trip 证据格式（强制 / 每个 D2-f/g 用例都产出）

> 目的：杜绝"门禁绿/stub 通过"的假性证据（v2.6 + D2-c-ii-C 两次教训）。**没有完整证据包 = 该用例视为未过**。

每个用例产出一个证据包目录 `docs/plans/reports/v2.7/d2-<case-id>/`，固定包含：

| 文件 | 内容 | 作用 |
|---|---|---|
| `meta.txt` | `claude --version`、执行时间、case-id、**完整 argv**、`mcp-config` 内容（token 脱敏为 `***`） | 证明跑的是真 claude + 正确 argv |
| `stdin.jsonl` | 实际注入 claude 的 stream-json **逐行**（任务简报 / 人回复 user message） | 通道①输入真凭 |
| `stdout.jsonl` | claude **真实** stdout **逐行原始**（未加工，含 system/assistant/user/result 嵌套包络） | 通道②输入真凭 |
| `parsed.jsonl` | 解析器产出的 AgentActivityEvent **逐行**；每行标注源 stdout 行号 | 解析正确性可逐行对账 |
| `assertions.md` | 期望 vs 实际逐条勾（PASS/FAIL）+ **Unknown 事件计数** | 判定依据 |
| `sideeffect.md` | （MCP 用例）AppService 端副作用前后对比：调用前后的 `GET` 响应 / DB 行 diff | 证"真打到 admin 且字段契约对"（非"转发了字段名"） |

**解析硬指标**：`parsed.jsonl` 对 `stdout.jsonl` 的**已知事件映射率 = 100%**，**EventUnknown 计数 = 0**（D2-c-ii-C 的②号断点正是真 claude 嵌套格式被全部判 Unknown → agent 输出几乎全丢）。任何 Unknown 行必须在 `assertions.md` 列出原始 JSON + 判 FAIL。

### § A.2 通道① 注入用例（stdin → claude 收到）

| Case | 步骤 | 期望 | 状态 |
|---|---|---|---|
| C1-init | start → 注入任务 brief（user message via stdin stream-json） | claude 实际收到并据此动作；编码正确（中文/多行/特殊字符不坏） | ✅ D2-c-ii-C 已过（claude 2.1.156，输入编码正确） |
| C1-wake | WorkItem `waiting_input` 时注入人回复（同会话 stdin） | 同一长驻会话续上下文、不拉新进程；`waiting_input → active` 新 AgentInteraction | 待 D2-e/f |
| C1-merge | `waiting_input` 时多条消息同时到 | 合并成**一轮**注入（OQ5(c)），非每条一次唤醒 | 待 D2-e/f |
| C1-buffer | `active`（忙）时来的消息 | 缓存，到下次 `waiting_input` 才注入（OQ5(b)） | 待 D2-e/f |
| C1-noself | Agent 自己发的消息 | **不**唤醒自己（OQ5(a)，无自环） | 待 D2-e/f |

### § A.3 通道② 输出解析用例（真 claude stdout → 正确 activity，零 Unknown）

> 用**真 claude 2.1.156 真实输出**做 testdata（round-trip 录制），非自编扁平格式。被验对象：`ParseStreamLine`（`internal/workerdaemon/claude_stream.go`）；断言 `StreamEvent.Type` ∈ `{system, assistant_text, thinking, tool_use, tool_result, result, rate_limit}`，**绝不为 `unknown`**。基底 fixture：`internal/workerdaemon/testdata/claude_2.1.156_stream.jsonl`。

| Case | 真 claude 输出形态 | 期望 `StreamEvent.Type` | 状态 / fixture |
|---|---|---|---|
| C2-system | 起始 `system` 包络（session/init） | `system` | ✅ 现 fixture 有 |
| C2-text | `assistant` message，`message.content[]` 含 text block | `assistant_text`（**不**自动进 Conversation，仅 ActivityEvent） | ✅ 现 fixture 有 |
| C2-thinking | thinking / reasoning block | `thinking` | ⏳ 需 fixture |
| C2-tooluse | `assistant` content[] 含 `tool_use`（name+input 嵌套） | `tool_use`，name/input 正确取自嵌套结构 | ⏳ 需 dev 补录 tool_use fixture |
| C2-toolresult | `user` content[] 含 `tool_result` | `tool_result`，内容正确 | ⏳ 需 dev 补录 tool_result fixture |
| C2-result | 顶层 `result` 包络（usage/session/末轮） | `result`，**不误判 unknown** | ✅ 现 fixture 有 |
| C2-ratelimit | 限流包络 | `rate_limit` | ⏳ 需 fixture（可选） |
| C2-zero-unknown | 一整轮真实会话 stdout 全量回放 | **`unknown` 计数 = 0**、映射率 100% | ⏳ D2-c-ii-D 落地后跑（②号断点回归） |

### § A.4 通道② MCP 工具契约用例矩阵（D2-g — 16 工具逐个，真实 admin 非 fake）

> **教训（b3）**：recording-fake 只证"转发了选定字段名"，证不了"字段名匹配 admin 契约"（`post_task_message` 的 `text` vs `content` bug 即此盲区）。**每个工具必须真 claude load mcp-config → 真调用 → 打到真 AppService 端点 → 断言成功 + 正确副作用（GET/DB 对照）。**

格式：`claude` 被引导调用工具 → MCPHost → AppService。每行产出 §A.1 证据包（含 `sideeffect.md`）。
**端点锚点（dev 确认 2026-05-30）**：JSON 工具统一 `POST /admin/agent-tools/<tool>`（工具名==路径段）；鉴权 = **worker bearer + `requireAgentOnWorker`**（token owner 取 worker、校验 `agent.worker_id == 该 worker`）。文件工具走 `/admin/files/...`（见下）。

| # | 工具 | 输入 | 期望副作用（真 admin 断言） | 关键负向/不变量 | 端点锚点 |
|---|---|---|---|---|---|
| 1 | `get_my_work` | — | 仅返回**自己**的 WorkItems | 读不到他 agent 的 WorkItem | `POST /admin/agent-tools/get_my_work` |
| 2 | `get_task` | own task_ref | 返回该 task | 非 own WorkItem / 跨 Project → 拒(403/404) | `POST /admin/agent-tools/get_task` |
| 3 | `create_task` | project_ref+标题 | 本 Project 下建 Task；经 outbox projector 建会话+同步 participant | 空/跨 ProjectID → 拒 | `POST /admin/agent-tools/create_task` |
| 4 | `assign_task` | task+assignee | 指派；建 AgentWorkItem | 跨域指派 → 拒 | `POST /admin/agent-tools/assign_task` |
| 5 | `reassign_task` | A→B | 旧 WorkItem superseded + 新建；**A 保留 sticky 订阅者(OQ13)** | A 未被移除 | `POST /admin/agent-tools/reassign_task` |
| 6 | `subscribe` | task+identity | 加订阅者（投递范围） | 跨域 → 拒 | `POST /admin/agent-tools/subscribe` |
| 7 | `unsubscribe` | task+identity | 移除订阅 | **creator 不可退订(OQ13)** | `POST /admin/agent-tools/unsubscribe` |
| 8 | `post_task_message` | text | 文本进 Task Conversation（**唯一**文本入会话路径）；**字段名匹配 admin 契约** | 验 `text`/`content` 契约(b3 bug 回归) | `POST /admin/agent-tools/post_task_message` |
| 9 | `request_input` | question | question 进会话 + WorkItem `waiting_input` | 触发唤醒闭环(§A.2) | `POST /admin/agent-tools/request_input` |
| 10 | `block_task` | reason(必填) | reason 进会话；Task `blocked`（仅从 `running`，OQ11） | 缺 reason → 拒；AgentWorkItem 无 blocked 态 | `POST /admin/agent-tools/block_task` |
| 11 | `complete_task` | summary? | 状态→`completed`（由 assignee/agent）；summary 进会话 | — | `POST /admin/agent-tools/complete_task` |
| 12 | `verify_task` | task | 由**非完成者**置 `verified` | **自验 → ErrSelfVerify(OQ4)** | `POST /admin/agent-tools/verify_task` |
| 13 | `get_issue` | issue_ref | 返回 issue（只读）；**严格 own-link**：仅可读"自持 WorkItem 的 task 所派生的 issue"（`task.derived_from_issue == issue_id`） | **项目成员但无派生 task 的 issue → 403 `not_in_issue_domain`**（project membership 是写门、不放宽读，b2/d-ii-B 已收紧） | `POST /admin/agent-tools/get_issue` |
| 14 | `upload_file` | filename+bytes | 返回 `ac://files/{ulid}` + transfer_uri；完成后可读 | ULID 身份、sha256 元数据 | `POST /admin/agent-tools/upload_file` → `PUT /admin/files/transfer/{id}` → `POST /admin/files/transfer/{id}/complete` |
| 15 | `download_file` | ac://files URI | 可达性授权内可下 | 跨域不可达 → 拒(fail-closed) | `GET /admin/files/{ulid}` |
| 16 | `attach_file` | msg+uri | 附件挂到消息、按元数据展示 | 授权 = **agent 自有域**（自持 WorkItem 的 task / 其派生 issue / agent:self / 自有 task 会话）；引用 project/org/他人 → 拒 | `POST /admin/agent-tools/attach_file` |
| (opt) | `report_activity` | — | 显式活动补充（多数自动捕获，可裁剪） | 标注是否实现 | 标注是否实现 |

### § A.5 D2-f cutover go/no-go 判定（翻开关前置硬门）

翻 cutover **前**必须全绿，任一未过或"从没跑过真 claude"= **禁止翻**（否则 agent 在跑但输出全丢 / 一个动作都做不了 = 静默失效）：

- [ ] **GATE-1** 通道①注入过真 claude（§A.2 C1-init ✅；C1-wake 唤醒注入过）。
- [ ] **GATE-2** 通道②解析 Unknown=0 / 映射率 100%（§A.3 C2-zero-unknown，真 claude 2.1.156 实输出回放）。
- [ ] **GATE-3** 通道② MCP 至少 `post_task_message` + `complete_task` + 1 文件工具真打 admin、副作用正确（§A.4 抽样；全量 16 在 D2-g 收口）。
- [ ] **GATE-4** 唤醒闭环真 claude 走通：`request_input` → 人回复注入 → claude 续（§A.2 C1-wake + §A.4 #9）。
- [ ] **GATE-5** argv 回归断言通过（§A.0：`--print` 在、无位置 prompt）。
- [ ] 证据：每条 GATE 附 §A.1 证据包链接。

### § A.6 域隔离 / 幂等 / fail-closed 负向用例（OQ4/OQ6 边界，并入 §A.4 各行执行）

- [ ] **域隔离**：任一工具操作非本 Project/Org 的对象 → 拒（跨 Project read/write/assign/file 全覆盖）。
- [ ] **管理面 human-only**：建 Project/Member/Agent/Worker 对 Agent 不可达（无对应 MCP 工具 / 调用被拒）。
- [ ] **no-self-verify**：`verify_task` 自己完成的 Task → ErrSelfVerify。
- [ ] **读域**：Agent 读范围严格限 own WorkItems（`get_task`/`get_my_work` 越界 → 拒）。
- [ ] **幂等**：命令带 `idempotency_key`，重投/重连 → 副作用恰一次（与 §1-D D1「重连不重复破坏」合验）。
- [ ] **fail-closed**：授权不确定/缺失 → 默认拒（不放过）。
- [ ] **stdout 不入会话**：claude 啰嗦中间输出只沉 ActivityEvent；断言 Conversation 仅含经 `post_task_message`/`request_input`/`block_task`/`complete_task` 显式携带的文本。
