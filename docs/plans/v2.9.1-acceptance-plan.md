# v2.9.1 验收方案(批量验收)

> PD 制定,**产品能力角度**(§5.6:验"产品为用户做了什么",独立于 PR/实现)。
> owner 2026-06-13 拍板:v2.9.1 = 全量(Thread + 收尾批次),全部合进 `v2.9.1` 分支后**一次性整体验收** → tag → promote `v2.9.1→main` → 部署。
> 三面:**PD §-1**(verify-not-trust,隔离 worktree 跑真 `make build`/`lint`/`go test`)+ **Tester**(data/API + class-guard)+ **Tester2**(run-real,真实例 + 真 LLM,computed-truth)。

## 0. 验收门(全绿才签)

集成好的 `v2.9.1` 上:`make build`(go + spa tsc-b + vite)· `make lint`(vet + spa-eslint + **no-raw-colors 现应绿**)· `go test ./...`(除既有 agentsupervisor socket env caveat)· web vitest 全过 · migration version 守卫一致(red-base 教训)。

## 1. 能力矩阵(逐条 → 验收点 → 谁验 → 证据)

| # | 能力 | 验收点 | 谁验 |
|---|---|---|---|
| A | **Thread 消息串** | 任一消息派生 thread;回复进同串、深度-1(server 强校验);reply_count 回写;Participants thread 列表;**thread 内 @agent → 真唤醒 + 回复落 thread 内(DB parent=root)**;has-activity 标记出现/查看消除;跨 org→404 | PD §-1 + Tester(class-guard)+ **Tester2 run-real** |
| B | **Task 状态机 7→5(ADR-0046)** | open/running/completed/discarded/reopened;**blocked=running 上注解**(block 写、unblock/resume/complete/discard 清);verified 删、completed 即终态;迁移 round-trip(存量 blocked→running 留 reason、verified→completed);node 无 blocked/verified;**无可死锁非法转移**(class-guard) | PD §-1 + Tester |
| C | **claimable + 内置池(ADR-0047)** | claimable 派生(未归档∧open∧已指派∧在plan∧node dispatched);assign≠claim;三段(真backlog 不可领 / 内置池可领·pull / 结构化 push);内置 plan 每项目唯一·常驻·禁边·随项目归档·自动建+backfill;**backfill:存量 assigned-backlog→内置池**;get_my_work 只返 claimable | PD §-1 + Tester + Tester2 run-real(看板三段) |
| D | **看板可见性** | backlog 默认排终态(completed/discarded)、`?status=` 可回看;**大 plan task 列表全显 + 搜索 + 可选 + 行内改 assignee + 移出 plan**(截断修);**归档项目的 task/issue 不在 task/issue 页**(显式筛该项目可见) | Tester + Tester2 run-real(12+ 节点大 plan) |
| E | **工具/门** | MCP `list_tasks`(项目 task,按 status/assignee 筛);eslint-gate 恢复(spa-eslint 绿);**no-raw-colors 全迁 token**(make lint 绿 + **both-mode AA 不退,含 locked 状态调色板**) | PD §-1 + Tester2(no-raw-colors both-mode computed-truth AA) |
| F | **恢复/运维** | `unblock_task`/`rerun_failed_node` 恢复 restart→死锁 blocked 的任务(cross-agent、project-member 守);stale 节点 assignee 回归自动重派(retry 上限) | PD §-1 + Tester(class-guard)+ Tester2 run-real |
| G | **频道归档** | archive(**不可逆**)→ 只读 409 + 默认排除 + `?status=archived` 可见(逐 kind);FE 归档分组(#317 样式) | Tester + Tester2 run-real |
| H | **Plan 详情 UX** | Task 号(T-num)在 DAG+列表;chat/DAG/Task 三 tab(默认 chat);长 DAG 紧凑布局;**依赖编辑(点击+键盘,cycle/self UI 拦,旧下拉移除,§4.2 可达)** | Tester2 run-real + PD §-1 |
| I | **横切质量** | both-mode AA≥4.5 无 alpha-tint;§4.2 可达;console=0;org-isolation 404(§5.7) | Tester2 + PD §-1 |

## 2. 批量流程

1. 第二波全合进 `v2.9.1`(IntegrationDev,superset 链 + 增量)→ 集成树 §0 门全绿。
2. **PD §-1**:对集成 trunk 跑真 make 门 + 核合并范围 + migration 守卫一致 + 上表 PD 项。
3. **Tester** data/API + class-guard(B/C/D/F/G + 各 §5.7/round-trip);**Tester2** run-real(A/C/D/E/F/G/H/I 的交互 + computed-truth 截图)。
4. findings 集中修(dev),回到对应面复验。
5. **PD 出最终验收报告(8 大能力签收)+ release-docs 定稿** → owner 拍 release 点 → IntegrationDev tag `v2.9.1` + promote `→main`(--no-ff)→ 部署(P0 unblock_task 此时才真生效)。

## 3. 签收 bar

所有能力 A–I 双面 GREEN(run-real 是 headline 硬门 §5.1)+ §0 门全绿 + 零未决 finding。任一红 = 不签、不 tag。
