# T1478 Evidence Report

Task: Subject access canonical/responsive/409 continuation after rejected SHA `f1b8c6b96445670c1a887a0d9bf1a0d68046b223`.

## Code changes

- Backend duplicate direct grants now fail as `ErrDuplicateAssignment`.
- `/api/access/batch/apply` preflights duplicate direct grants and returns HTTP `409` with `error=duplicate_direct_grant`.
- Permission error mapping exposes the same `409 duplicate_direct_grant` contract for direct permission APIs.
- Access subject source chains wrap through visible canonical blocks:
  - `membership:<team>` -> `Team Role <role>` -> `RAM Role <role_id>`
  - `direct binding` -> `RAM Role <role_id>` -> `<permission> on <resource>`
- Access tables/cards now use `min-w-0`, `max-w-full`, and local horizontal overflow wrappers to prevent 1280px KPI/detail/sidebar overlap.

## Frozen behavior retained

- `not_applicable` remains counted separately and is not folded into deny.
- Direct custom grants remain unioned into effective permissions.
- Active grant revoke/refetch/audit flows remain covered.
- Missing/unauthenticated permission state remains fail-closed through existing authorization/API tests.
- Explicit deny/fail-closed resolver paths remain covered by authorization service tests.

## Raw evidence

- `go-test-count1.log`: `go test -count=1 ./internal/authorization ./internal/webconsole/api`
- `go-access-focused.log`: focused Access API tests including duplicate direct `org.read` 409 and direct union/revoke.
- `go-authorization-focused.log`: focused authorization service fail-closed/revoke/custom-role coverage.
- `web-tsc.log`: `pnpm --dir web exec tsc -p tsconfig.app.json --noEmit`
- `web-access-vitest.log`: `pnpm --dir web exec vitest run src/pages/Access.test.tsx`
- `subject-access-1684x934-viewport.png`: browser viewport screenshot.
- `subject-access-1280x720-viewport.png`: browser viewport screenshot.
- `subject-access-1684x934.png` and `subject-access-1280x720.png`: full-page browser screenshots.
- `metrics-1684x934.json` and `metrics-1280x720.json`: DOM `clientWidth`/`scrollWidth`, request status, and viewport metrics.
- `metrics-summary.json`: derived summary proving all harness requests returned 200 and no measured horizontal overflow at 1280x720 or 1684x934.
- `console-*.txt`, `errors-*.txt`: browser console/page errors. No console error entries and no page errors were captured; only Vite debug/info lines appear.
- `responsive-1280-vs-1684-overlay.png`, `responsive-1280-vs-1684-diff.png`, `responsive-diff-stats.json`: responsive overlay/diff generated by resizing the 1684 viewport image to 1280x720 and comparing it with the 1280 viewport image.
- `canonical-search.txt`: repository search for T1458 canonical artifacts.
- `screenshot-identify.txt`: ImageMagick dimensions for viewport screenshots.

## Canonical artifact note

The task requires alignment to T1458 canonical SHA256 `9d903c702ac15c6cbb08e4b46f58c60fee40b9ef22924628ffc9daf606f1ce3`. This workspace does not contain that canonical screenshot/artifact. `canonical-search.txt` is empty after searching the repository while excluding `web/node_modules` and this T1478 evidence directory. The responsive overlay/diff files therefore compare T1478's two viewport captures only; they are not presented as canonical-baseline proof.

## Instance note

This executor is intentionally isolated from agent-center control-plane credentials and was instructed not to use center/MCP/admin/database/socket fallbacks. The committed browser evidence uses a local Vite acceptance harness (`web/t1478-acceptance.html`) that renders the real `Access` component with deterministic local fetch responses. It proves the UI and request/error behavior locally, but it is not a fresh owner/reviewer production instance.
