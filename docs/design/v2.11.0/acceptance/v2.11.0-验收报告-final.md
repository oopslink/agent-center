# v2.11.0 发布验收报告（final） — Agent 提醒 / 定时（Reminder · I4）

**裁定：CONDITIONAL（条件通过）** · 执行：PD · 日期：2026-06-20 · 目标 commit：`dev/remainder@72d46403`

## 一句话结论
后端（领域/调度/投递/MCP+API）、授权红线、构建硬门 **全绿且证据 in-tree**；UI 对设计稿有 **2 处实质偏离**（待 owner fix-or-waive），真浏览器 UI 截图 + deployed-smoke **待补** —— 这三项补齐后方可终判 GO 并合 main。

## 做了什么（人话）
本周期交付「Agent 提醒/定时」：用户可在 Reminders 页给**同 project 的 agent** 设一次性或周期（cron）提醒，到点把提醒内容作为一条 directed message 唤醒目标 agent；支持暂停/恢复/取消、结束条件、上次未投完则跳过本次；并修复了自我提醒被误判跨 project（T229）、真投递回写 outcome（T239/T240）。

## 验收结论（详见同目录 `../ACCEPTANCE.md`）
- **逐任务**：T205/T206 ✅、T207 ⚠️（UI 偏离）、T229/T239/T240 ✅。
- **授权红线**：跨 project 拒 / owner 豁免 / class-guard / 可见性 / 自我提醒 —— 全过 ✅。
- **构建门**：`make lint` + `make build` exit 0 ✅（`evidence/build-gate-make-lint-build.log`）。
- **测试**：reminder 全域 + admin API 测试全绿 ✅（`evidence/go-tests-reminder.txt`，含调度/时区/overlap/投递/红线 26+ 用例逐条 PASS）。

## 阻挡终判 GO 的项
1. **F-A** 左 rail 缺「提醒我的」范围；**F-B** 新建弹窗缺「以本人身份创建提醒文本」开关 —— 对照 `mockups/reminder-mockup-v0.1-I4.png`，需 owner 裁 fix or waive。
2. **真浏览器 UI run-real 截图**（§4.3）与 **deployed-smoke ≥1**（§8）待补。

## 证据清单（verify-in-tree）
- `mockups/reminder-mockup-v0.1-I4.png` — owner 提供的设计稿（本次入仓）
- `evidence/build-gate-make-lint-build.log` — 构建硬门输出（lint+build exit 0）
- `evidence/go-tests-reminder.txt` — 逐用例 PASS（576 行）
- `../ACCEPTANCE.md` — 逐任务清单 + 红线 + findings

## 下一步
owner 裁 F-A/F-B（fix or waive）→（若 fix）开 FE 任务修到 1:1 复签 → 补 UI 截图 + smoke → PD 合 `dev/remainder → main` + 打 tag `v2.11.0` + 清开发分支。
