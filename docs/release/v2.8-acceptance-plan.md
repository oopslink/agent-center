# v2.8 Conversation UX Epic — Acceptance Plan

> **Scope:** v2.8 (1) conversation UX overhaul — three phases per `docs/design/v2.8-conversation-ux-research.md` §5.8.
> **Per @oopslink directive (2026-06-05):** v2.8 needs proper acceptance flow + plan deliverable.
> **Owners:** Tester (data/API/correctness) · Tester2 (UI/UX) · PD (cross-phase + R/Q + scaffold).
> **Reviewed by:** Dev2 · Dev · IntegrationDev · @oopslink.
> **Companion docs:** `docs/rules/acceptance-methodology.md` (T-1..T-9 discipline + false-alarm rule-out) · `docs/rules/ux-standards.md` (#192 / a11y) · `docs/rules/v2.7-delivery-process.md` §11/§19/§20/§21 (release engineering).

This plan applies the established v2.7.1 / #258 acceptance pattern (real-instance + double-PASS + sequence merge + content-delta verify) to each of v2.8's three implementation phases. Each phase gates on its own acceptance run; no phase can ship without passing its row.

---

## Binding principle — 验收独立于开发实现，验收忠于产品设计

Per @oopslink directive 2026-06-05 + `acceptance-plan-independence` memory:

**Acceptance source of truth = product design** (PR #168 §5 design proposal + the D1-D4 / R1-R3 / Q1 / `#`/`@` 4-layer lock + this plan's §0 anchors). **NOT** Dev implementation code, NOT Dev's branch, NOT what the unit tests cover. Tester / Tester2 acceptance gates are drafted **cold from spec, with zero Dev design participation and zero Dev-branch reference**, then validated only against the product design documents. Dev implementation is the **object under test**; acceptance assumes it could be implemented incorrectly and sweeps every entity behavior the design requires.

This prevents:
- (a) **Unit-green / real-broken** — testing what we built rather than what we should have built.
- (b) **Acceptance drift** — acceptance silently following code instead of leading it.
- (c) **Dev/Tester collusion** — gates softened to make implementation pass; design intent quietly erodes.

**Operational consequence** for filling Tester / Tester2 owned sections:
- Cite **`PR #168 §5.x` / `D1-D4` / `R1-R3` / `#/@` 4-layer lock / this plan's §0** when stating a gate. Do **not** cite Dev branch code, file paths, or function names as the gate's source.
- Tester2 UI gates such as "picker has `role=combobox` + `aria-expanded` + `aria-activedescendant`" cite `§5.3` + `§5.6` (design), **not** the DOM structure Dev's component happens to emit.
- Tester data gates such as "artifact CRUD `/api/artifacts?org_slug=` with versioning" cite `§5.4` + `§3.1` of this plan (which mirrors the design), **not** the route table Dev wires.
- This applies to every owner section in every phase. The scaffold §0 anchors below are the binding source; everything else descends from them.

(Tester #258 black-box acceptance and Tester2 #259 PilotDeck source-dive already followed this discipline. Codifying it here ensures every v2.8 phase reaffirms it.)

---

## 0. Anchors (from PR #168 §5 design proposal + @oopslink 2026-06-05 D1-D4/R1-R3 lock)

| Anchor | Value (LOCKED) |
|---|---|
| D1 (Thesis 5) | Reuse channel+thread; **no topic dimension** (work-item-as-message). |
| D2 (Thesis 1) | All-at-once 4-surface MessageList rewrite (channel/DM/task-thread/issue-thread); **hard cutover, no feature flag** — strict no-middle-state. |
| D3 (Thesis 3) | v2.8 keeps **directed-wake only** (no `@everyone`); wake-governance → v2.9 #262. |
| D4 (Thesis 4) | **Side Surface 1.0** — dual-display (inline collapsed/expanded + side panel always full) + versioning + deeplink + tab strip + `aria-modal=false` non-blocking + configurable auto-promote thresholds. |
| R1 mitigation | Hard cutover (no feature flag) + per-phase normal v2.7.1-style acceptance + **E2E mandatory** + lint enforced from day-0. |
| R2 thresholds | Client default + per-org/conversation server override (configurable, not hardcoded). |
| R3 unread model | Slack-style `(conversation_id, user_id, last_read_seq)` tuple — standard pattern. |
| Q1 phase ordering | **Sequential P1 → P2 → P3** (per @oopslink); contract for `<MessageBlock>` frozen early in P1 anyway, to inform P2 design even under sequential. |
| #/@ design | 4-layer lock: input (δ) picker+typed+hash-id all; storage = structured ref token; render = chip; paste (III) auto-convert + internal fidelity. |

**Open-question convergence (PR #169 cross-review round, @oopslink locked 2026-06-05)**:

- **Q-T1 (P1 agent unread/badge?)** → **human-only**. The `(conversation_id, user_id, last_read_seq)` tuple's `user_id` resolves to **human identities only**; agents do not accumulate unread/badge state because their attention model is directed-wake (D3) — being mentioned/DM'd triggers execution, not "I'll read it later". Tester (§2.2) verifies a human-only unread model + also verifies agents are NOT incorrectly counted into any badge. Tester2 (§2.3) verifies no agent-row badge DOM. Source: §F Thesis-3 + D3 directed-wake lock.
- **Q-T2 (P2 artifact Delete?)** → **deferred to v2.9 via task #267**. v2.8 P2 ships artifact as append-only / no Delete. Rationale: (a) preserves complete version history per D4 versioning intent (Delete would break history-back + deeplink); (b) minimal P2 scope — engineering load on P2 is already significant; (c) early observation that what users typically want is a new artifact version, which is already covered. **T-9 CRUD-completeness gate passes via explicit deferral**: §3.4 must literally write "delete deferred v2.9 (#267) — rationale: preserve version history per D4 + minimal scope" so there is no implicit gap (§11 explicit-pointer rule). Tester (§3.2) gate: "if not present in §3.4 verbatim, P2 acceptance fails T-9". v2.9 design phase re-evaluates whether to introduce soft-delete (preferred if any) vs hard-delete (rejected — breaks deeplink/history-back).
- **Q-T3 (P3 ref → deleted entity)** → **token persists + chip renders `"(deleted)"`**. Layer-2 structured ref token is permanent (never drifts); when the underlying entity is deleted/removed, the chip renders `"(deleted)"` (reuse the v2.7.1 #215 DM peer `"(deleted)"` pattern), `hover` still shows full id (does not leak raw ULID in chrome — #192), and `click` is graceful (entity-not-found / friendly 404 — does not crash). Tester (§4.2) verifies API ref token persistence; Tester2 (§4.3) verifies chip text content + hover + click behavior + DOM token persistence (computed-truth). Source: §5.3 Layer-2 lock + #192 EntityRef deleted-path + #215 DM peer pattern.

---

## 1. Cross-phase gates (apply to EVERY phase)

These gates apply to **every** phase release. No phase ships without each row green.

### 1.1 Methodology discipline (Tester / Tester2 both)
- [ ] **Computed-truth assertion** (§1.1): all UI assertions read `getComputedStyle` / rendered DOM / HTTP code / byte assertion — never class-name inference.
- [ ] **Run-real-usage on real install** (§1): every acceptance run uses a real `install` codepath (real test-instance from `#255` helper; for P2/P3 also `#257 --with-seed`), never hand-crafted config / SQL shortcut.
- [ ] **Real product default prefix** (§1.2): path/discovery/layout features must run under `~/.agent-center` (not `/tmp`-isolated) where applicable.
- [ ] **False-alarm rule-out** (§1.1): every red/failing observation triggers harness-error + environment-noise check before being reported as a finding (concrete root cause, not "probably noise").
- [ ] **Deployed-smoke (≥1 per phase)** (testing.md §2.3): one full acceptance pass against a real deployed build (not unit-test only).
- [ ] **T-1..T-9 discipline** (`acceptance-methodology.md`): real defaults / real product prefix / real chained tools / shared-instance protocol / evidence-in-tree / completeness sweep / regression sweep / CRUD-completeness.

### 1.2 Quality red-lines (lint-enforceable from day-0)
Per PR #168 §5.6/§5.7 — these gates are enforced in CI, not just in review:

| Red line | Mechanism (CI) | Owner |
|---|---|---|
| no raw ULID in chrome (#192) | extend existing zero-raw-id regex sweep test | Tester2 verify; Dev2 implement |
| no `dangerouslySetInnerHTML` on unsanitized SVG/HTML | custom ESLint rule | Tester2 verify |
| no native `<dialog>` / `window.confirm` | custom ESLint rule (no-restricted-syntax) | Tester2 verify |
| ARIA picker / combobox required props | `eslint-plugin-jsx-a11y` recommended ruleset + `role-has-required-aria-props` | Tester2 verify |
| link-unfurl SSRF guard (P2 if applicable) | backend test asserts internal IP rejected + private-IP block list | Tester verify |
| `Content-Disposition: attachment` on downloads | header assertion test | Tester verify |

CI gate: all of the above green on every PR; no merge without.

### 1.3 D2 hard-cutover discipline (no-middle-state enforcement)
- [ ] **Pre-cutover snapshot**: backup of old component still buildable on a tag before merge (so revert is `git checkout <tag> -- <paths>` if needed, not a guessed undo).
- [ ] **Post-cutover smoke**: production build runs on real install codepath with no half-state observable in the 4 surfaces (channel/DM/task-thread/issue-thread all on the new MessageList; old component fully removed).
- [ ] **Rollback plan** (in commit/PR description): explicit single-commit revert path verified in advance (PR description includes "to roll back: `git revert <merge sha>`").
- [ ] **No flag fallback** — there is no feature flag; cutover is binary; old code paths deleted in same merge as new code paths land.
- [ ] **Single-merge unit constraint** (Dev2 cross-review): for hard-cutover phases that delete old code, the deletion + new code MUST land in **one single merge commit** so `git revert <merge sha>` is a complete undo. Splitting across multiple commits breaks the no-flag rollback story.

### 1.4 E2E mandatory (Playwright real browser)
- [ ] E2E suite covers each phase's user-visible flows on a real browser against a real running instance.
- [ ] CI: E2E green required for merge.

### 1.5 Evidence-in-tree (§13 + §20 + §21)
- [ ] Every acceptance run produces an evidence report at `docs/release/evidence/v28-p<N>-<phase>-<run>-acceptance-report.md`.
- [ ] Screenshots / fixtures live in `docs/release/evidence/screenshots/` and are committed **in the same PR** as the corresponding finding.
- [ ] Pre-merge: IntegrationDev verifies `git show <branch-tip>:<doc>` content-delta matches PR description claims (not just file presence).
- [ ] Post-merge: same content-delta check against `main` to confirm landed state.

### 1.6 Cross-phase regression sweep (every phase + final)
Each phase's acceptance must include a regression sweep against all previously-shipped v2.8 phases plus v2.7.1 features touched:

| When acceptance for... | Regression sweep covers... |
|---|---|
| **P1 (MessageList)** | v2.7.1 #258 Issues/Tasks list (chip / org_ref / unassigned), #224 wake projector still wakes on @mention, #245 org-seq display |
| **P2 (Side Surface)** | P1 (4-surface MessageList still renders correctly with Side Surface added), v2.7.1 #216 activity timeline, #242 ProjectDetail chip cross-page consistency |
| **P3 (#/@ picker)** | P1 (composer integration), P2 (Side Surface artifact still works under new picker), v2.7.1 wake projector backward-compat (old plain-text @mention messages still wake) |

---

## 2. Phase 1 — Surface-agnostic MessageList (#264, D1+D2+R3)

### 2.1 What ships
- One `<ConversationView>` + shared `<MessageList>` / `<MessageRow>` / `<Composer>` component for **4 surfaces** (channel / DM / task-thread / issue-thread).
- `<MessageBlock>` interface frozen **in first 1-2 weeks of P1** (props: payload, isCollapsed, onExpand, onSendToSide) — feeds P2 design even under sequential ordering (Dev2 prep). **Internal checkpoint deliverable** (Dev2 cross-review): the contract MUST land as a tracked artifact (TypeScript interface in a referenceable location + a short markdown spec section) before P2 can begin design — `<MessageBlock>` "frozen" means the interface signature is committed and documented, not just verbally agreed.
- Backend: unread/read-position model `(conversation_id, user_id, last_read_seq)` tuple; sidebar badge count derived from this; thread follow/unfollow UI maps to existing CLI semantics.
- task-thread/issue-thread: work-item-as-message — discussion IS a thread on the work-item message, no separate "comments" model.

### 2.2 Tester data/API gates (owner: @AgentCenterTester)

> Applies the cross-phase invariants (§1) + the real-2-org seed model from #258. Gate = all rows PASS on a real install.

1. **Unread/read-position model (R3 tuple `(conversation_id, user_id, last_read_seq)`)**
   - badge count == actual unread (messages `seq > last_read_seq`) — exact: send N, mark-read at K, assert N−K.
   - mark-read **advances** last_read_seq; **idempotent** (re-mark = no change); **monotonic** (never regresses).
   - **per-(user,channel) isolation**: user A's read position ≠ user B's; channel X ≠ channel Y; no cross-tuple bleed.
   - new message increments unread for **all members except author**.
   - **Q-T1 LOCK (human-only)**: `user` = human identities only; **agents are NOT in the badge/unread model** (agent attention = directed-wake, D3). Verify an agent member shows **no** unread badge; @mention of an agent **wakes** it (does not accumulate unread).
2. **Thread follow/unfollow API**: follow → delivered/counted; unfollow → stops; state per-(user,thread); auto-follow on participate / @mention (verify the rule).
3. **work-item-as-message (D1)**: task/issue detail thread = a thread **on the work-item message** (same conversation+message model — **NOT a separate comments table**). Verify via API the thread messages live under the work-item message id; no parallel comments store.
4. **4-surface API same-source**: the same conversation returns an **identical** message payload for channel / DM / task-thread / issue-thread → one `<MessageList>` data contract, no per-surface divergence.
5. **`<MessageBlock>` contract frozen** (props payload / isCollapsed / onExpand / onSendToSide) at the P1 1-2-week checkpoint — verify it is stable + documented (feeds P2 design under sequential ordering).
6. **D2 hard-cutover (R1 = NO feature flag)**: the 4 surfaces switch to the unified MessageList **atomically** — verify **no half-migrated state** (no point where some surfaces use new + some old; no dual-write to two models; old component fully deleted in the same merge). Rollback = **`git revert <merge sha>`** — verify the revert path restores the prior per-surface state cleanly (there is no flag to flip back).
7. **run-real / computed-truth / false-alarm**: real install + real browser; send / scroll(pagination) / mention / react / attach across **all 4 surfaces** with real messages. Badge/unread read from API+rendered truth (not assumed). Any unread-count mismatch → rule out test timing / eventual-consistency / polling **before** flagging an off-by-one (§1.1).

### 2.3 Tester2 UI/UX gates (owner: @AgentCenterTester2)

**Source of truth:** §0 anchors (D1 / D2 / R3 + Q-T1) + design proposal §5.8 (P1 surface-agnostic) + §5.1/§5.2. Drafted cold from spec per §13 binding principle — the Dev component DOM is the object-under-test, **not** the gate's source. All assertions are **computed-truth** (`getComputedStyle` → `rgb(...)`, `.value` property, rendered DOM, HTTP code), never class-name inference. Every red/failing observation passes **false-alarm rule-out** (harness/env root-cause) before it is recorded as a finding.

**G2.3-A — per-surface × 4 dimensions** (channel / DM / task-thread / issue-thread, each surface independently swept):
- **A1 render parity** — the same `<MessageList>` / `<MessageRow>` / `<Composer>` renders on all 4 surfaces; verify message order, author EntityRef (name + hover id), timestamp, and body are present and structurally identical per surface (one screenshot per surface as evidence). Source: D2 single-component lock.
- **A2 #192 zero-raw-id sweep** — full-page chrome text scan per surface: raw-entity-id regex `(agent|project|task|issue|worker|dm|channel|mem|organization|user)-[0-9a-f]{8}` AND raw-ULID regex `01[KJ][0-9A-HJKMNP-TV-Z]{24}` both return **0 hits** in chrome (message bodies are content-exempt per the #192 boundary); `console` errors = 0. Any hit is reproduced + root-caused (false-alarm rule-out), not class-name guessed.
- **A3 #258 chip consistency** — status/role chips render via the same StatusChip component; computed-truth: chip background computed color matches the #258 token (read `rgb(...)`, not a class-name `danger`/`red` guess) consistent with v2.7.1 Issues/Tasks lists.
- **A4 empty / loading / unassigned states** — each surface shows a real empty state when no messages (not blank, not a stuck spinner); loading shows a skeleton; absent/unassigned author shows a friendly placeholder (no raw id, no literal `undefined`).

**G2.3-B — Q-T1 human-only badge** (locked §0):
- **B1** — unread/badge UI renders **only for human members**; agent rows carry **NO badge DOM** (computed-truth: query an agent row → assert no badge/count node present). Source: §0 Q-T1 + D3 directed-wake.
- **B2** — sidebar badge count derives from the `(conversation_id, user_id, last_read_seq)` tuple with `user`=human: a human's unread count increments on a new message and clears on read; an agent identity never accrues a count.

**G2.3-C — D2 hard-cutover no-middle-state (visual)**:
- **C1** — all 4 surfaces simultaneously on the new MessageList; no surface shows the old component and no mixed/half-migrated rendering (no-middle-state). Source: D2 hard-cutover §0 + §1.3.
- **C2 rollback UI verification** — per §1.3 there is **no feature-flag toggle** anywhere in the UI (computed-truth: settings/conversation views contain no MessageList toggle control); the documented rollback is `git revert <merge sha>`. After a simulated revert build, the 4 surfaces render the pre-cutover component cleanly with no half-state. (Pairs with Tester §2.2 #5 + IntegrationDev; UI side owns "no flag control exists".)

**G2.3-D — cross-phase regression** (§1.6 P1 row): v2.7.1 #258 Issues/Tasks list (chip / org_ref / unassigned) still renders; #224 wake still fires on `@mention` (mention renders + message delivered); #245 org-seq (`T<n>`/`I<n>`) display intact.

**G2.3-E — E2E (Playwright real browser)**: scripted flow per surface — sign in (display_name + passcode), open each of the 4 surfaces, send a message, scroll, `@mention`, verify render — green on a real running instance.

### 2.4 Phase 1 blocking gates (must all pass to ship)
- All cross-phase gates (§1) green.
- 4 surfaces all on new MessageList; old component gone (hard cutover).
- E2E covers all 4 surfaces × key flows.
- Tester data/API + Tester2 UI/UX double-PASS, sequence-merge per #258 pattern.
- Evidence report in tree.

---

## 3. Phase 2 — Side Surface 1.0 (#265, D4+R2)

### 3.1 What ships
- `<SideSurfaceHost>` + `<ArtifactPanel>` resizable right-side panel; `<ArtifactCard>` inline collapsed; tab strip for multi-artifact; history back.
- Dual-display: inline collapsed/expanded + side panel; non-exclusive (both can be open).
- Auto-promote thresholds: client default (code > 20 lines / JSON > 5 KB / doc > 200 lines) + per-org/conversation server override (configurable).
- Backend: `artifact` entity new schema (id ULID / kind / payload / version / message_id / conversation_id / created_at) + `artifact_versions` table; 3 endpoints (POST/GET/PATCH `/api/artifacts?org_slug=`); deeplink routing `/<surface>?artifact=<id>`.
- a11y: `aria-modal=false`, no focus steal, Esc closes, Tab traverses normally, full keyboard nav including Ctrl+arrow resize.

**Schema-change discipline gate** (Dev cross-review, per `conventions.md` §9.4): the `artifact` + `artifact_versions` migration is a NEW backend schema and counts as a Schema 工作单位. Independent of runtime CRUD acceptance, the migration must satisfy:
- Migration up + down both work cleanly.
- The 5 schema_version assertion sites updated in sync (see `conventions.md` §9.4 for the canonical list).
- `collectKnownKeys` allowlist updated if any new config key is introduced.
- **Real cross-version upgrade smoke** on a non-empty database (round-trip an existing v2.7.1 install through the upgrade and verify migrated data + new artifact CRUD work) — this is the #245 / #251 lesson: migrations need a "real upgrade verified" gate independent of unit migration tests.

### 3.4 v2.9-deferred items (explicit — no implicit gaps, per T-9)

- **artifact Delete** — deferred to v2.9 via task **#267** per @oopslink lock (2026-06-05). Rationale: preserves complete version history per D4 versioning intent (delete would break history-back + deeplink); minimal P2 scope; observed product preference for new-version-over-delete is already covered by versioning. v2.9 will re-evaluate; if introduced then, the design-phase preference is soft-delete/archive over hard-delete (the latter breaks deeplink + history-back).
- **Cross-conversation artifact share** — deferred v2.9 (per §5.9 design proposal).
- **Multi-user collaborative artifact edit** — deferred v2.9 (per §5.9).
- **Mermaid / chart live render** — deferred v2.9 (per §5.9 — v2.8 supports code / diff / image / JSON only).
- **Attachment-level reactions** — deferred v2.9 (per §5.9).

P2 acceptance T-9 CRUD-completeness gate: every item in this section MUST appear with explicit rationale; **any unlisted deferral fails the gate** (T-9 catches implicit gaps).

### 3.2 Tester data/API gates (owner: @AgentCenterTester)

1. **artifact CRUD**: `POST / GET / PATCH /api/artifacts?org_slug=` — create / read / update; schema fields present (id=ULID / kind / payload / version / message_id / conversation_id / created_at).
2. **Versioning**: agent edit → **new version** row in `artifact_versions`; `version` **monotonic**; history-back returns prior versions in order; latest is default; PATCH **creates a version** (not in-place clobber).
3. **Cross-org no-leak**: artifact org-scoped via `?org_slug=`; 2-org → orgA cannot GET/list orgB's artifact (404/403); list scoped to caller org (#258 org-scope axis re-applied to artifacts).
4. **Linkage integrity**: `message_id` + `conversation_id` correct (artifact ties to its source message + conversation); orphan/dangling guarded.
5. **Auto-promote threshold (R2)**: client default (code > 20 lines / JSON > 5 KB / doc > 200 lines) + **server override per-org/conversation is genuinely CONFIGURABLE (not hardcoded)** — change the server threshold → promotion behavior changes (verify the override path is real, not a constant).
6. **Deeplink**: `/<surface>?artifact=<id>` resolves; reload preserves state; bad/foreign artifact-id → clean 404 (no leak).
7. **Q-T2 LOCK — append-only, NO Delete in v2.8 (@oopslink); T-9 CRUD-completeness gate passes via EXPLICIT documentation, not implementation**: verify the **v2.9-deferred items** block (§3 deferred list) explicitly states "artifact delete deferred → v2.9 **#267** + reason (preserve version history / versioning covers undo)"; **no delete endpoint expected** — verify it is deliberately absent **and documented**, NOT a silent gap (the #255 / retrospective §11 / capability-completeness discipline). Verify versioning truly covers the undo intent (edit→new version, not delete) and append-only does not orphan deeplink / history-back.
8. **Schema-change discipline (artifact + artifact_versions = NEW migration — separate gate from runtime CRUD; conventions.md §9.4 / #245·#251 lesson)**: migration up+down present; the **5 `schema_version` assertion sites bumped in lockstep** (migrator_test / round_trip / handlers_migrate const + test / integration_test); `collectKnownKeys` updated if a new config key is added; **real cross-version upgrade smoke** (install the prior version with real data → upgrade → confirm the migration ran on **real old data**, NOT just an empty-DB round-trip). "Did the migration run on real prior-version data" is its own gate, independent of runtime-smoke and CRUD.
9. **run-real / false-alarm**: real artifact from a real agent edit (or real-API seeded) + real browser dual-display; versioning/threshold anomalies → rule out my seed/timing first (§1.1).

### 3.3 Tester2 UI/UX gates (owner: @AgentCenterTester2)

**Source of truth:** §0 anchors (D4 + R2 + Q-T2) + design proposal §5.4 (Side Surface 1.0 dual-display) + §5.6/§5.7 (a11y / security). Cold from spec per §13. Computed-truth + false-alarm rule-out throughout.

**G3.3-A — non-modal a11y** (D4 core — the defining P2 gate):
- **A1 `aria-modal=false`** — the side-panel host root carries `aria-modal="false"` (computed-truth: read the attribute on the panel root); it is **not** a modal. Source: D4 lock.
- **A2 no focus-trap / no focus steal** — opening the panel does **not** move focus into it; the composer/message input retains focus (computed-truth: `document.activeElement` unchanged after open). Tab from the conversation traverses **into and back out of** the panel naturally — Tab past the panel's last control returns to page flow (no trap).
- **A3 Esc closes** — Esc closes the panel without closing the underlying conversation.
- **A4 full keyboard incl. resize** — every panel action is keyboard-reachable; **Ctrl+arrow resizes** the panel (computed-truth: panel width style changes on Ctrl+arrow).
- **A5 not-color-only / labeled** — panel controls have `aria-label`; state is never conveyed by color alone.

**G3.3-B — dual-display two-way toggle**: inline card collapsed ↔ expanded toggles correctly; "send to side" promotes to the panel; panel and inline are **non-exclusive** (both can be open simultaneously — computed-truth: both DOM nodes present). Source: D4 dual-display.

**G3.3-C — auto-promote thresholds real test** (R2 configurable): client default (code > 20 lines / JSON > 5 KB / doc > 200 lines) auto-promotes to the panel; the **per-org/conversation server override actually applies** — change the server config, reload, and verify a previously-inline artifact now auto-promotes (or vice-versa). Confirm the change is the config taking effect, not cache/coincidence (false-alarm rule-out). NOT hardcoded. Source: R2 lock.

**G3.3-D — multi-artifact tab strip**: multiple artifacts open as tabs; switching tabs swaps panel content; tab close works; tab labels show the artifact name (no raw id — #192).

**G3.3-E — deeplink reload preserves state**: `/<surface>?artifact=<id>` reload re-opens the panel to that artifact and preserves panel state (computed-truth: after reload, panel shows the same artifact). The artifact-id in the URL is **id-as-content** (#192-exempt in the URL). Source: D4 deeplink.

**G3.3-F — Q-T2 append-only (no delete) UI verify** (T-9, locked §0):
- **F1** — there is **no delete/archive control** anywhere in the artifact UI (append-only); computed-truth: no delete button/menu item in the artifact card or panel. Editing produces a **new version** (version indicator increments). Source: §0 Q-T2 + §5.4.
- **F2 T-9 doc-evidence gate** — verify **§3.4 of this plan** literally documents "artifact Delete deferred v2.9 (#267) + rationale"; the gate passes on the **explicit deferral present in-tree at the P2 acceptance tag** (§21 doc-evidence-same-commit), NOT on a delete feature existing. Any artifact behavior neither shipped nor listed in §3.4 = **T-9 fail** (implicit gap).
- **F3** — versioning covers "undo": editing → new version, and the old version is still reachable via history-back (validates Q-T2 rationale (a) — absence of delete does not break deeplink/history).

**G3.3-G — #192 full-page sweep** on P2 surfaces (panel + tab strip + inline cards): raw-entity-id + raw-ULID = 0 in chrome (deeplink artifact-id exempt as id-as-content); `console` = 0.

**G3.3-H — no native dialog**: any confirm/discard in P2 uses in-app non-modal UI, **not** `window.confirm` / native `<dialog>` (§1.2 red-line, runtime-verified).

**G3.3-I — cross-phase regression** (§1.6 P2 row): P1 4-surface MessageList still renders with the Side Surface added; v2.7.1 #216 activity timeline intact; #242 ProjectDetail chip cross-page consistency intact.

**G3.3-J — E2E (Playwright)**: open artifact → promote to panel → resize (Ctrl+arrow) → switch tabs → deeplink reload → verify, on a real browser.

### 3.5 Phase 2 blocking gates
- All cross-phase gates (§1) green.
- a11y `aria-modal=false` AND focus behavior correct (non-modal not modal).
- artifact schema + versioning + deeplink fully functional.
- v2.9-deferred items explicitly NOT shipped (cross-conversation share / collab edit / Mermaid / attachment-react) — verify they're absent.
- Tester + Tester2 double-PASS, sequence merge.

---

## 4. Phase 3 — #/@ picker (#266, #248 + #/@ 4-layer spec)

### 4.1 What ships
- Composer ARIA combobox picker (trigger on `@` or `#` literal); fuzzy-match candidate list; keyboard nav (↑↓/Enter/Tab/Esc); icon-typed agent vs human.
- Backend: ref-candidate resolver endpoint (prefix → candidates, scoped to current org via `?org_slug=`).
- Storage: structured ref token (kind + stable id + user-typed form preserved).
- Render: chip with friendly name; hover full id card; click navigates to detail; status chip for work-item refs (StatusChip + orgRefToken reuse); EntityRef (#192) for boundary.
- #/@ accepts: typed friendly name (`@Bot Nine`) OR org seq (`#T34`) OR hash id (`#channel-abc123`) — all parsed and rendered as same chip.
- Paste: smart auto-convert exact matches (Slack-style) + internal copy preserves rich refs (fidelity).
- `#254 identityRef(kind, id)` helper extracted early in P3 (prevents `agent:`/`user:` prefix drop — closes #240 root cause).
- wake-projector string-match backward-compat preserved (old messages still wake correctly).

### 4.2 Tester data/API gates (owner: @AgentCenterTester)

1. **ref-resolver endpoint**: prefix → candidate entity list; **6 types** (channel / task / issue / project / agent / user); **both identifier dimensions accepted** per type (hash id + friendly name / org-seq `T<n>`·`I<n>` / member-id).
2. **Cross-org no-leak + nested `?org_slug=`**: resolver org-scoped — candidates ONLY from the caller's org; 2-org → no cross-bleed.
3. **Structured ref token "never drifts" (Layer 2, core)**: ref stored as `{kind, stable-id, typed-form}`; **rename display_name → the ref STILL resolves** (display changes, id permanent); change the org-seq display → stable-id unchanged → ref intact. Verify by mutating display_name / seq **after** creating a ref and confirming it still resolves to the same entity.
4. **Paste fidelity round-trip**: smart-paste converts text → structured refs; **internal copy round-trips** (copy a message containing refs → paste → refs preserved as structured tokens, not flattened to plain text).
5. **Wake-projector backward-compat**: old string-match wake still works (old messages / API-posted text with `@name` still wake the agent) — P3 must NOT break the existing wake path.
6. **Q-T3 LOCK — ref → deleted entity**: the structured ref token **persists** (Layer-2 never-drift, even after the entity is deleted); the chip renders **"(deleted)"** (reuse the v2.7.1 #215 DM-peer pattern), hover does **not** leak a raw ULID (#192 chrome), click is graceful (friendly 404, no crash); the resolver only surfaces **live** entities as candidates. computed-truth: assert the token text is truly retained in the DOM. (Design spec: design §5.3 / merged PR #170.)
7. **run-real / false-alarm**: ref resolution via real API; picker driven in a real browser (deep picker UI = Tester2; I sample the resolver↔render same-source); false-alarm rule-out applied (§1.1).

### 4.3 Tester2 UI/UX gates (owner: @AgentCenterTester2)

**Source of truth:** §0 anchors (`#/@` 4-layer + Q-T3) + design proposal §5.3 (`#/@` picker + deleted-ref path) + §5.6. Cold from spec per §13. Computed-truth + false-alarm rule-out throughout.

**G4.3-A — ARIA combobox a11y** (the defining P3 gate): triggering on `@`/`#` opens a candidate list with **`role=combobox`** on the input, **`role=listbox`** on the list, **`aria-expanded`** toggling true/false, and **`aria-activedescendant`** pointing to the highlighted option (computed-truth: read attributes, assert they track ↑↓ navigation). ↑↓ moves selection, Enter selects, Tab selects/closes, Esc closes **without inserting**. Source: §5.3 picker spec + §1.2 jsx-a11y red-line.

**G4.3-B — chip render (name + hover id, #192)**: a selected ref renders as a **chip showing the friendly name** (e.g. `@Bot Nine`, `#T34`); **hover shows the full id card** (id-as-content in the hover, allowed); full-page sweep = **0 raw ULID in chrome**. Verify all three entry forms — typed friendly name (`@Bot Nine`) / org seq (`#T34`) / hash id (`#channel-abc123`) — render the **same chip** (computed-truth: chip text + structure identical). Source: §5.3 render=chip + #192.

**G4.3-C — Q-T3 ref → deleted entity** (locked §0, cite §5.3):
- **C1** — a ref to a since-deleted entity renders chip text **`"(deleted)"`** (computed-truth: chip `.textContent` contains `"(deleted)"`, reusing the v2.7.1 #215 DM-peer pattern) — NOT blank, NOT a raw id.
- **C2** — hover does **not** leak a raw ULID (#192 chrome) even for a deleted entity.
- **C3** — click is **graceful**: a friendly entity-not-found / 404, no crash (computed-truth: navigation lands on a friendly state, no `console` error / white screen).
- **C4** — the structured **ref token persists in the DOM** (Layer-2 never-drifts — the token survives entity deletion; computed-truth: the token element is still present carrying the stored id). Source: §0 Q-T3 + §5.3.

**G4.3-D — active-status chip realtime** (no-middle-state): a work-item ref chip with a status shows the **current** status or no status — never a stale/half-rendered status. Source: §5.3 status chip + no-middle-state discipline.

**G4.3-E — smart paste, no surprise dialog**: pasting text containing exact-match refs auto-converts (Slack-style) **without a native confirm dialog** (§1.2 no-native-dialog red-line; computed-truth: no `window.confirm` fired, no native `<dialog>`), frictionless. Cross-org / nonexistent / ambiguous matches do **not** auto-convert to false refs — ambiguous paste stays plain text or offers in-app non-modal disambiguation (pairs with Tester §4.2 false-ref check).

**G4.3-F — full keyboard navigation**: the entire picker flow (trigger → navigate → select → edit/remove chip) is reachable by keyboard only.

**G4.3-G — #192 sweep + console = 0** across the composer and rendered messages carrying chips.

**G4.3-H — cross-phase regression** (§1.6 P3 row): P1 composer integration intact; P2 Side Surface artifact works under the new picker; v2.7.1 wake-projector backward-compat — old plain-text `@mention` messages still wake and render.

**G4.3-I — E2E (Playwright)**: type `@`, navigate the picker by keyboard, select, send, verify the chip renders with name + hover id; paste a ref; verify a deleted-ref chip — on a real browser.

### 4.4 Phase 3 blocking gates
- All cross-phase gates (§1) green.
- 6 entity types × 2 identifier dimensions × all 4 entry paths (picker / typed friendly / typed hash / pasted) all parse correctly.
- `#254 identityRef` helper extracted and used by all `@`/assignee render call sites (no per-component string concat of prefix).
- wake-projector backward-compat verified on real legacy messages.
- Smart paste doesn't generate false refs on cross-org / nonexistent / ambiguous matches.
- Tester + Tester2 double-PASS, sequence merge.

---

## 5. Final v2.8 (1) epic close-out

After P3 ships and acceptance double-PASS:
- [ ] All 3 phases shipped and acceptance-reported in tree.
- [ ] All cross-phase regression sweeps green for every phase.
- [ ] All lint rules from §1.2 are active in CI and green.
- [ ] Final v2.7.1 / v2.8 trunk diff verified consistent with §5.9 (v2.9 deferred set actually deferred — no scope creep).
- [ ] @oopslink final review against §4.4 D1-D4 + §5.10 R1-R3 mitigation as actually implemented.
- [ ] Close v2.8 epic; open v2.9 follow-up tasks from §5.9 explicit deferral list (#256 / #257-phase-2 #261 / #262 / cross-conversation artifact share / collab edit / Mermaid / attachment-react / topic dimension evaluation / SSE-dependent items).

---

## Appendix: Owner sections to be filled

> Each section below is a placeholder for the owner to fill via independent doc-PR (§19 worktree, no collision with this scaffold-level edits).

### Tester data/API/correctness segment (owner: @AgentCenterTester)

**Method / tooling (reused from #258 acceptance — real-2-org black-box):**
- **Real install**: build the phase binary (`make build`) → `agent-center install test-instance` (#255 helper; for P2/P3 `--with-seed` from #257-p1). Isolated namespace + dynamic ports (avoid :7000). Real launchd, real subprocess = deployed-smoke (§1, testing.md §2.3).
- **Real-2-org seed via real API** (not SQL): signup → create 2nd org → projects → issues/tasks → assign. **Gotchas paid down in #258** (use them, don't re-debug): signup body `{display_name, passcode (6 digits), email (required), organization_name, organization_slug}` sets a **Secure** `ac_session` cookie (scripts over http must re-attach it manually — browsers special-case localhost, urllib does not); **nested create also needs `?org_slug=`** (`POST /api/projects/{id}/issues|tasks?org_slug=`); assign `{"assignee":"user:<uid>"}` (uid from `GET /api/auth/me`.identity_id); signin body = `{display_name, passcode}` (NOT email); issue state-machine rejects illegal transitions (e.g. open→closed) — use a legal path.
- **API↔UI same-source**: real Playwright browser (cached chromium); SPA holds an SSE connection so `networkidle` hangs → use `domcontentloaded` + `wait_for_selector(table row)`. Inject the session cookie into the context, drive the new pages, assert rendered rows == API totals/fields (the #258 same-source method).
- **Org-scope no-leak**: 2-org (acme+globex), assert each org's endpoint returns zero cross-org rows **both directions**.
- **Aggregation/count gates** (where applicable): controlled seeded counts so `total == items == sum-of-per-source` is exactly assertable.

**Cross-phase discipline (every phase):** computed-truth (rendered/API truth, not class guess) · **false-alarm rule-out** (a red result is a hypothesis — rule out harness error + env-noise [live procs / SQLite WAL / timing] to a concrete root cause before flagging; cf. #255 D1 live-prod-DB-churn ≠ pollution, E4 `find -exec grep -l` exit-code bug) · evidence committed into the phase PR commit + `git ls-tree` / `git show` content-delta verify · post-push `git ls-remote` self-verify.

**Open-question resolution (locked, applied in the gates above):** Q-T1 unread/badge = human-only (agents directed-wake) · Q-T2 artifact append-only, no delete in v2.8 → v2.9 #267 (T-9 gate = verify explicit deferral, not impl) · Q-T3 ref→deleted = token-retained + "(deleted)" chip.

**Per-phase evidence path**: `docs/release/evidence/v28-p<N>-<phase>-acceptance-report.md` (+ screenshots same-commit).

### Tester2 UI/UX segment (owner: @AgentCenterTester2)

Full per-phase gates are inline at **§2.3 (P1) / §3.3 (P2) / §4.3 (P3)**. This segment records the cross-cutting Tester2 harness + rigor lenses every phase run applies.

**Acceptance harness**
- Real-browser verification via **Playwright** (python `sync_playwright`, headless chromium, 1440×1100) against a real running test-instance (`#255` helper; P2/P3 also `#257 --with-seed` / `--with-agent` for seeded entities + real agent tool events).
- Sign-in = **Display name + passcode** (form fields `#display_name` + `#passcode`), **not** email (seeded owner display_name `"Acme Owner"` / passcode `123456`). [#258 lesson]
- Evidence: screenshots → `docs/release/evidence/screenshots/`; report → `docs/release/evidence/v28-p<N>-<phase>-<run>-acceptance-report.md`; committed **in the same PR** as the finding (§1.5 / §21 doc-evidence-same-commit).

**Cross-cutting rigor lenses (applied every phase)**
- **Computed-truth** — `getComputedStyle` (color as `rgb(...)`, not class token), `.value` property (not the attribute), rendered DOM, HTTP code; never class-name inference. [#250 Reset-red lesson]
- **#192 zero-raw-id sweep** — raw-entity-id + raw-ULID regex = 0 in chrome (content / id-as-content exempt); EntityRef name + hover-id everywhere.
- **False-alarm rule-out (闻报先证伪)** — every red observation gets a harness/env root-cause check before it becomes a finding.
- **No-middle-state** — loading / empty / failure / stale states all explicitly verified.
- **a11y** — ARIA roles/props, full keyboard, not-color-only, Esc/focus discipline.
- **Lint red-lines (§1.2) runtime-verified** — no raw ULID / no `dangerouslySetInnerHTML` on unsanitized / no native dialog / ARIA combobox props — verified at runtime in addition to CI lint.
- **CRUD-completeness (T-9)** — every entity's full lifecycle UI verified; any deferral must be explicit in-tree (e.g. §3.4 artifact-delete → #267), never a silent gap.
