# v2.8 Acceptance Report

**Release**: v2.8 (alias of v2.7.1 trunk)
**Final shipped commit**: `8780511` (含 PR #193 post-100% runtime-finding micro-patch)
**v2.8 100% commit**: `ae4697a` (PR #181 → PR #192 main scope chain)
**Acceptance scope**: 12 main PRs + 1 cleanup PR + 1 micro-patch PR (14 PRs total)
**Date**: 2026-06-06
**Authored by**: @AgentCenterPD (compile) with evidence from @AgentCenterDev / @AgentCenterDev2 / @AgentCenterTester / @AgentCenterTester2 / @AgentCenterIntegrationDev

---

## 1. Overview

v2.8 cycle delivered the **(1) UX 会话页面改进** main scope + **(2) Global Issues+Tasks sidebar** + **(3) test-instance helper** + 5 piggyback features (agent archive epic / Worker detail page / Activity epic / markdown rendering + code折叠 / #/@ picker). All shipped through 12 main PRs + 1 cleanup + 1 post-ship runtime-finding micro-patch, with **zero production-affecting findings across the entire chain** at code+test+data+UI layers. Consolidated runtime end-to-end gates all PASS or runtime-bounded-but-mechanism-verified.

The pattern proven 15 times in this cycle (mock=contract day-0 → impl → three-face independent acceptance → 0 finding → merge) is now codified as institutional discipline for future releases.

## 2. User-visible features (用户视角)

| # | Feature | Trunk commit |
|---|---------|--------------|
| 1 | 测试实例工具: `install test-instance --with-agent` 一键起含真 claude-opus-4-8 agent 的测试 instance (prod 零足迹, 同机 dev 友好) | (v2.7.1 inherited via #261) |
| 2 | 左侧 Issues+Tasks 全局侧栏: 跨频道工作项一处看 + 含 assignee 列 + (archived) chip | (v2.7.1 inherited via #258 + #260) |
| 3 | 会话页面统一架构 + 未读 badge + 关注按钮: 4-surface (channel / DM / task线程 / issue线程) 同一 React 组件 ConversationView + 未读数字 / 红 mention badge / 关注 toggle (#264 P1) | `e8f9b5c` |
| 4 | agent 归档后端: agent 软删 (soft archive) — agent 行保留 + (archived) chip 数据面就位 + 释放 worker binding + hard-delete 改 admin-only + 严格两步 (running → 409 must-stop-first) (#272) | `0d4e8c9` |
| 5 | agent 4 button 全图标 + ConfirmModal + Start 修 + ESLint guard: 启停按钮全 SVG icon + 破坏性操作二次确认 + #271 Start 偶发回文字 bug 修 + 静态 lint 防回退 (#270 + #271 + ESLint infra) | `7a2def5` |
| 6 | (archived) chip 渲染: agent 归档后任务列表显 "(archived)" 标识 (前端 #270 follow-up + 后端 #184 DTO 嵌套字段) | `7d08736` (chip), `3c91ef9` (DTO) |
| 7 | Agent Activity 翻页后端: cursor-based pagination + (a) presence-check + 并发 append 闸 + ULID DESC 稳定单调 (#274 backend) | `8abed1b` |
| 8 | 完整 markdown 渲染 + 长 code 块自动折叠 + XSS 安全闸: react-markdown + remark-gfm + 严格 escape (no rehype-raw) + CollapsibleCodeBlock 共组件 13 a11y locks (#276) | `920dece` |
| 9 | Agent Activity 前端 part 1: Load older 按钮 + Checking messages 自动折叠 + 跨页 re-group 不裂 (#274 part 1) | `15df164` |
| 10 | Agent Activity tool 富渲染: CAT_TOOL_USE/RESULT + SVG icons (放大镜 / 扳手 / terminal / ✓ / ✗) + tool_result 内联输出复用 CollapsibleCodeBlock (#274 part 2) | `4c2cf39` |
| 11 | Worker 详情页 4-tab: Profile (5 真字段 + 5 v2.9 占位) + Bound Agents (复用 #258 表格 + restart + in-UI hint "无 unbind 请 Open → Archive") + Management (rename + Install + Re-mint + Remove 含 bound-agent 警示) + Activity placeholder + shared useTablistKeyboard hook (manual activation 回写 #228) (#273) | `53e90b6` |
| 12 | 会话输入框 #/@ picker: ARIA combobox + 输 @ 弹人 / 输 # 弹频道 + 选完插 `@<name> ` / `#<channel> ` 文本 token + trailing-space 保证 word-boundary wake 真触发 (#275) | `ae4697a` (v2.8 100%) |

**Post-100% runtime收口** (PR #193 micro-patch):
- Esc 真关闭 picker (was no-op due to keyup-sync 同帧重开 unit-vs-runtime gap)
- 选项 hover-only id (was 暴露 visible 裸 id 违 #192 chrome standard)
- Final shipped at `8780511`

## 3. PR ship chain

```
a741780 (trunk before v2.8) → 
0d4e8c9 (#181 #272 agent archive backend) →
e8f9b5c (#182 #264 P1 frontend) →
7a2def5 (#183 #270 core agent buttons) →
3c91ef9 (#184 assignee_lifecycle DTO) →
7d08736 (#185 (archived) chip render) →
8abed1b (#186 #274 cursor pagination backend) →
920dece (#187 #276 markdown + code折叠) →
15df164 (#188 #274 frontend part 1) →
4c2cf39 (#189 #274 part 2 tool rich render) →
53e90b6 (#190 #273 WorkerDetail) →
5c7c07e (#191 cleanup stray package-lock.json) →
ae4697a (#192 #275 #/@ picker — v2.8 100%) →
8780511 (#193 #275 runtime fix — final shipped)
```

= **14 PRs** (12 main + 1 cleanup + 1 micro-patch) across the cycle, all `--no-ff` merged with §19 race coord + §11 main+v2.7.1 ff-sync.

## 4. Per-PR acceptance summary

All 12 main PRs and 2 follow-up PRs received:
- **§-1 PD intent approve** (in-PR GitHub comment + scope/lock verification)
- **Three-face independent acceptance** (Tester data/API + Tester2 §3.3/§4.3 UI/a11y + Dev/Dev2 code review backup)
- **Zero findings** in the layer required to merge (production-affecting)
- **Per-PR §-acceptance evidence** logged in thread + GitHub PR comments

Highlight: PR #178 (#268 backend) through PR #192 (#275 picker) all merged on first attempt with zero rework PRs (other than PR #193 which addresses **post-100%** runtime findings caught by consolidated runtime acceptance, a discipline-positive outcome).

## 5. Consolidated runtime end-to-end matrix

Conducted on instance `acme-t3 @ 127.0.0.1:58687` (build `8780511`, on-demand built via `make build` + `install test-instance --with-agent --workers 2`, same-machine direct-access; 跨机 reproducible via recipe in `evidence/v28-consolidated-runtime/build-owner/build-summary.md`).

### Data/API面 (Tester, 7/7 PASS direct on shipped `8780511` + ae4697a 首轮 5/5)

| PR | Item | Verdict | Evidence |
|----|------|---------|----------|
| #182 | P1 badges 同源 | ✅ PASS | unread=1 + mention=1 + followed=True + mention≤unread invariant |
| #185 | (archived) chip data | ✅ PASS | `task.assignee.assignee_lifecycle='archived'` 嵌套字段 + enrichAssignee 正确 join archived agent |
| #190 | dangling-binding | ✅ PASS | DELETE worker → agent worker_id 悬空保留 + availability=unavailable + GET 200 不崩 |
| #192 | picker wake round-trip | ✅ PASS | `@<name> ` mention_count++ + word-boundary 正反例活验坐实 (`@Peer Bob` ✓ mention 0→1 / `@Peer Bobxyz` ✗ mention 不变 — 多词名 wake confirmed) |
| #188 | cursor 机制 | ✅ PASS (mechanism) | limit=10 多页链重组无 gap/dup + next_cursor=null 末页态. >50 events Load-older UI trigger = fakeagent test-double bounded, cursor 机制 triple-证 (code + data + runtime) |

Evidence:
- `docs/release/evidence/v28-consolidated-runtime/data-api/runtime-data-api-evidence.txt` (ae4697a 首轮 5/5 含 2 false-alarm 自证伪过程)
- `docs/release/evidence/v28-consolidated-runtime/data-api/shipped-build-8780511-reconfirm.txt` (8780511 direct-on-shipped 7/7, 一次过零 false-alarm — 上轮教训全应用)

### UI/a11y面 (Tester2, 7/7 PASS direct on shipped `8780511` + minor 2/2 PASS + 2 runtime findings → PR #193 fix → runtime re-verified)

| PR | Item | Verdict | Highlight |
|----|------|---------|-----------|
| #182 | P1 badges | ✅ PASS | sidebar mention-badge="1" + aria-label "1 unread, 1 mention" (not-color-only/SR) |
| #185 | (archived) chip | ✅ PASS | OrgWorkItemsView 渲 "(archived)" chip (读嵌套 assignee.assignee_lifecycle) |
| #187 | XSS 真注入 | ✅ PASS | `<script>` / `<img onerror>` / `javascript:` link → 全 neutralize + `window.__pwn` undefined |
| #188 | Load-older | ✅ PASS | "No more activity" 终态明确 + Checking-fold disclosure aria-expanded |
| #189 | tool 富渲染 | ✅ PASS | SVG icon (非 emoji) + tool_result `data-tool-status` |
| #190 | WorkerDetail | ✅ PASS | manual tab arrow-nav 活验 (ArrowRight focus-only 不切 tab / Enter 激活) + dangling fallback `worker-<hex>` handle (Lock 2 坐实) |
| #192 | picker | ✅ PASS | 2 runtime findings (Esc no-op / 裸 id 可见) → PR #193 micro-patch ship + runtime 验 on shipped `8780511` 全 PASS (Esc 真关 + aria-expanded=false @40ms+540ms 无 reopen / 可见文本仅名字, 裸 id 仅 hover) |

Evidence: `docs/release/evidence/v28-consolidated-runtime/ui-a11y/` 含截图 00-65 共 26 张 (00-50 = ae4697a 首轮含 2 findings 抓取证据 / 60-65 = shipped `8780511` re-verify) + `runtime-ui-a11y-evidence.md` (待 §21 chain-commit per [[verify-evidence-actually-in-tagged-tree]])

### Build owner summary (Dev2)

2 on-demand 实例经 `make build` + `install test-instance --with-agent --workers 2`: ae4697a (首轮) + shipped `8780511` (direct-on-shipped). 经真 center HTTP API 执行 6-item seed checklist (含 >50 events 多 task dispatch / archived agent assignee fixture / dangling worker fixture / peer-mention+follow / multi-human channels / XSS injection 备料), 使 Tester + Tester2 各 7/7 direct-on-shipped — 含 **#268-author-auto-advance-aware 顺序** (mention 放最后, 保 #182 unread/mention 不被作者自发清). Build-owner run-real 证 #274 activity 契约 `{activity, next_cursor}` 与 useInfiniteQuery 一致 (no mock-drift) + #182/#185 嵌套字段读对. **#188 >50-events Load-older UI = fakeagent test-double bounded** (cursor 三重证 code+data+runtime; 真 claude agent 查大库自然产 >50). 1 self-retracted false-alarm (#185 嵌套 vs 扁平字段 check 错, Tester live verify rule-out 双向证伪).

Evidence: `docs/release/evidence/v28-consolidated-runtime/build-owner/build-summary.md`

### API↔UI 同源 jointly confirmed

Tester data + Tester2 UI evidence 各自独立 + 互相 compare = 双面闭合. 同一 #182 false-alarm (#268 author-auto-advance / channel-viewing-clear-unread 顺序) 在两侧 convergent rule-out, 钉死为 harness 错非产品.

## 6. Institutional discipline applied

| Discipline | Applied to | Evidence |
|-----------|-----------|----------|
| 5-way bidirectional design↔acceptance binding | #168/#170/#174/#176/#180 contract docs ↔ #263 §3.4/§4.3/§3.3/§2.2/§3.2 acceptance plan | All 5 design PRs ship, all 5 acceptance sections honored at PR-level §-acceptance |
| §13 acceptance-independent-of-implementation | Every PR | Tester/Tester2 自旋独立 seed + 独立 compare, never trust Dev/Dev2 self-report |
| §19 worktree isolation + ls-remote 自验 | Every PR | IntegrationDev chain consistency |
| §20 content-delta verify on rebase | PR #184 / PR #185 | Tester 0-byte diff verification post-rebase |
| §21 doc-evidence same-commit (planned for this report) | This report + evidence | Tester [[verify-evidence-actually-in-tagged-tree]] v2.7.1 lesson — `git ls-tree` 验在树 |
| Right-layer enforcement (Lint vs Hook vs Test) | #270 ESLint guard / #273 useTablistKeyboard hook / #187 XSS Test §-acceptance / #189 no-emoji Test (a11y.test.tsx) | 选最高效已覆盖层, 不重复多层 redundant guard |
| False-alarm self-rule-out (闻报先证伪) | Tester #185 嵌套字段 false-alarm / Tester2 #275 row-expand minor / convergent #182 #268-trap | "Red = hypothesis, rule out harness/env before claiming product finding" |
| Tester2 independent injection | XSS (PR #187), grouping×pagination (PR #188), manual activation (PR #190), aria-activedescendant-by-id (PR #192), trailing-space mentionToken (PR #192) | 不信 Dev TDD 自述, 独立注入 verify |
| T-9 deferred-with-pointer (no 空挂) | #273 Profile 5/9 字段 v2.9 占位 / #273 Activity tab "Coming v2.9" / #267 artifact Delete v2.9 | 显式 defer vs 隐式缺口 |
| no-middle-state (epic 不切碎) | #274 epic (part 1 + part 2 一起 ship not 推一半) / #273 name→详情/rename 收口在增量 6 一致 / #270/#272 epic 闭环 4 PR / blocks[] 全推 v2.9 一气呵成 | @oopslink 2026-06-03 立的原则全程应用 |
| Frontend ship gate (`tsc -b` > noEmit, `npm run build`) | #189 (Dev2 catch unused-param) | 真 ship gate = full build, 非 partial check |
| 4-step institutional chain (catch → RCA → re-catch → close) | #270/#272 (Tester DTO catch → Tester2 #271 RCA → Tester code-grounded re-verify → Dev DTO + Dev2 chip ship) | 跨 4 PR 不断 catch-fix-verify-close, 0 production finding |
| Consolidated runtime value 闭环 | PR #193 micro-patch | unit-green → runtime-catches → fix-at-correct-layer → test-at-correct-layer |

## 7. v2.9 deferred-with-pointer 清单

Per Dev surface (msg=e0f4103d) + cycle-wide PD lock:

1. **worker-info-reporting** (#273 Profile 5/9 字段): hostname / OS / arch / agent-center binary version / install path 缺后端源. v2.8 = Profile 5 真 + 5 "Coming in v2.9" 占位. v2.9 epic = worker 端 enroll 时上报 + workforce schema + DTO.
2. **worker-removal cascade / unbind**: 现 DELETE /workers/{id} silent-orphan (bound agent worker_id 悬空保留, availability 降, 不崩). v2.8 = Restart + Archive-via-AgentDetail (PD pick (a) unbind=N/A) + UI hint discoverability. v2.9 cascade epic = worker 删 → bound-agent auto-archive / unbind/rebind endpoint.
3. **blocks[] + P2 #265 artifacts impl** (increment 4 "C" 决策 @oopslink 拍): B1 derive text/code/diff + B2 cross-BC tool_use/tool_result merge + #265 artifacts impl (off #269 已 ship 的 schema 契约). v2.9 epic = 一气呵成 (epic 不切碎).
4. **mention id-based format (option B)** (#275 rename-safe): `@<id>` token 需改 mention.Present 两处 caller (read_state.go:294 + wake_projector.go:509, 现 displayName-based). v2.8 = name-based A (`@<name> ` 文本 token + trailing-space, 零后端). v2.9 = rename-safe upgrade if user feedback shows demand.
5. **Worker Activity tab** (#273): GET /api/workers/{id}/activity 缺. v2.8 = "Coming v2.9" 占位 (与 worker-info-reporting 同 epic candidate).
6. **mention friendly-handle @username** (PR #193 finding 2 derived): MemberResult 无保证 handle, v2.8 选项 hover-only 裸 id. v2.9 = friendly-handle (e.g. derived from email or user-set handle) for visible secondary.

(All deferred items match @oopslink's "block 不引入" + "Activity 改进进 v2.8" + "Artifact 后续讨论" decisions logged on 2026-06-06.)

## 8. Scope-decisions trace (key @oopslink + PD picks during cycle)

| Decision | Picked | Trace | Rationale |
|----------|--------|-------|-----------|
| v2.8 scope main = (1) UX / (2) sidebar / (3) test-instance helper | confirmed by @oopslink | v2.8 epic open | Multiple scope items deferred to maintain epic 不切碎 |
| #270 (I) soft-archive + (b) strict two-step + hard-delete admin-only | (I) + (b) | @oopslink 2026-06-06 08:53 | agent CRUD 闭环 + history visibility + 误触防护 |
| Increment 4 富块 scope = C (defer entirely to v2.9 epic) | C | @oopslink 2026-06-06 09:54 | epic 不切碎 (B1 切 P2 artifacts 工作跨版本); P1+(2)+(3) 80% value already in v2.8 |
| #273 Remove worker = hard-delete + ConfirmModal | hard | PD 2026-06-06 14:47 day-0 | worker = runtime host (re-enroll 即重置) ≠ agent persona (audit value); 与现 DELETE endpoint 一致 |
| #273 Profile gap = T-9 defer 5 字段 v2.9 + 5 真渲 | (a) defer with "Coming v2.9" pointer | PD 14:50 | Tester T-9 lens; backend gap 非 trivial; defer 比空挂诚实 |
| #273 unbind=N/A (use Archive via AgentDetail) | (a) accept N/A | PD 16:25 + Dev2 corroborate | binding immutable in #272, Archive 已清 worker binding (semantic 已存); +in-UI hint discoverability |
| #275 #/@ picker format = A 文本 token (not B id-bearing) | A | PD 16:46 day-0 + Dev grounding | (b)-lightweight scope; wake matcher (#178) 已 handle displayName; rename-safe = (b) trade-off (与 Slack/Discord 一致) |
| #275 markdown rendering = A (full markdown via react-markdown) | A | @oopslink 2026-06-06 12:26 | (B code-fence 抽取也可, A 更彻底; 用户 explicit ask) |
| #276 XSS strict escape (Q-A1) | (i) strict | @oopslink 12:28 + Tester2 "security MUST" framing | 安全 red-line, agent markdown UNTRUSTED, no rehype-raw |
| #276 GFM 全开 (Q-A2) | (ii) GFM full | @oopslink 12:28 | 表格 / task list / strikethrough / autolink for agent-common output |
| #276 syntax highlighting | defer v2.9 | Dev2 propose + PD agree | 重 dep (shiki/prism) + spec 没要求; v2.8 = language badge text + monospace 足 |
| #275 manual tab activation (not automatic) | manual | PD 15:03 (Dev2 catch pre-PR) | async tab 箭头扫触 fetch 风暴问题; WAI-ARIA APG 推荐 manual for async panels |
| Team workload rebalance (Dev2 → Dev) | confirmed by @oopslink | 14:34 + 14:38 | Dev2 heavy frontend / Dev backend chain 完成后 idle; Dev 接 #273 + #275 frontend |
| Roadmap "团队模版功能" placement | v3 推迟 章节 (locked) | @oopslink 2026-06-06 14:00 | Long-term vision; v2.x 聚焦单元能力先 |
| Final report timing | (b) 等 consolidated runtime 完成 | @oopslink 17:34 | 含 runtime evidence + 截图 vs 现在 partial draft |

## 9. Known constraints + caveats

- **No persistent v2.8 runtime instance**: on-demand build via `make build` + `install test-instance --with-agent --workers 2` (reproducible from any clean checkout of `8780511`). Recipe in `evidence/v28-consolidated-runtime/build-owner/build-summary.md`.
- **fakeagent test-double output bounded**: `#188` Load-older >50-events UI trigger is bounded by fakeagent emitter limit in test instances. Cursor mechanism itself is triple-证 (code TDD + data/API black-box at limit=10 multi-page + runtime). Production real-claude agent (查询大代码库 / 多 tool call) naturally produces >50 events.
- **Prod 零足迹**: All test instances install to `~/.agent-center-test/<id>/` namespace, no contamination of production `~/.agent-center/`. Cleanup via `uninstall test-instance --id <id>`.
- **Acceptance evidence chain-commit per §21**: This report + Tester data evidence + Tester2 UI evidence + Dev2 build summary should be committed in a single commit chain per Tester [[verify-evidence-actually-in-tagged-tree]] (v2.7.1 lesson: untracked evidence files get git-clean'd, ls-tree verify post-commit).

## 10. Tag + ship recommendation

**Recommend** tagging trunk `8780511` (or `ae4697a` if 100% main scope preferred, with PR #193 as patch-tag follow-up) as `v2.8` upon @oopslink approval. Both shipped to `refs/heads/main` and `refs/heads/v2.7.1`; immutable tag `refs/tags/v2.7.1` (72fd296 → bdc9818) preserved.

---

**End of report. — v2.8 cycle 团队胜利, 不是单人胜利. 🎊**
