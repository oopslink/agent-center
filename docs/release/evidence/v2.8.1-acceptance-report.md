# v2.8.1 Acceptance Report

**Release:** v2.8.1 — #278 D pull-model rollout + @oopslink polish/feature set
**Trunk at draft:** v2.8.1 @ 89f3376
**Status:** PARTIAL (Phase 3 in progress — completed faces committed now; pending faces final-appended after Dev2 Edit-Task modal FE ship)

Acceptance is structured as a multi-face cross-check: Backend Data/API (Tester), FE UI (Tester2), Real-LLM Behavioral (Tester2), with PR-level §-1 sign-off (PD) per the team's independent-acceptance discipline (PD intent + Tester criteria, Dev zero design involvement).

---

## § 1 — Executive Summary

_(PD writes after all faces fold.)_

---

## § 2 — Backend Data/API Acceptance (Tester)

**Scope:** independent black-box + white-box data/API acceptance of the v2.8.1 backend (the #278 D pull-model rollout + the @oopslink polish/feature set), conducted by the Tester lane independent of Dev's design. **Method per PR:** build the PR independently → black-box behaviour + **full `go test ./...`** (defense-in-depth ship-gate) → white-box code-confirm of the invariant → **content-delta** the merged trunk file vs my-verified commit (byte-identical) so the GO is provably commit-bound. Pre-existing `workerdaemon`/`agentsupervisor` PTY/IO flakies classified as env-noise (not regressions), ruled out per finding.

### 2.1 Verdict summary — 11/11 backend verifies GO + landed + content-delta byte-identical to trunk

| # | Capability | PR | Landed @ | Verdict |
|---|---|---|---|---|
| 1 | A6 dual-stream (get_my_unread scope + mark_seen) | #206 | trunk | ✅ GO |
| 2 | A7 reconciler / 30-min watchdog (stuck→release, NO-FALSE-KILL, race-symmetric) | #209 | trunk | ✅ GO |
| 3 | A8 cutover (pure pull-model; start_work sole activation) | #212 | trunk | ✅ GO |
| 4 | (b) task↔WI sync (failed ANY cause → Task.Block) | #214 | trunk | ✅ GO |
| 5 | Version endpoint/panel | #217 | trunk | ✅ GO |
| 6 | State-machine model fix (assigned→metadata; canceled/withdrawn→discarded; migration 0052) | #221 | trunk | ✅ GO |
| 7 | Force-delete agent/worker (orphan-sweep + unbind) | #223 | 9c72061 | ✅ GO |
| 8 | Filter time-range (tz-safe, no off-by-one) | #224 | c10e3cc | ✅ GO |
| 8b | Filter 4-way combo (status∧project∧assignee∧date, AND-gate, OR-within-set) | (same endpoint) | c10e3cc | ✅ GO (code-confirmed) |
| 9 | Free state-machine SetStatus (any-valid, no adjacency, POST /status) | #229 | 0273650 | ✅ GO |
| 10 | Edit-Task (tags + status_changed_at + batch PATCH atomic) | #232 | 89f3376 | ✅ GO |
| 11 | PR7 — D pull-model Go integration suite (6/6) | #231 | 91f3e17 | ✅ MERGED |

### 2.2 Headline: @oopslink's "multiple agents running simultaneously" bug — eradicated, FOUR-FOLD坐实

The single-active invariant (one agent ⇒ at most one active WorkItem) is proven across four independent layers:

1. **Code race-gate** — PR1 race-gate + migration 0051 single-active index.
2. **Real-LLM behavioral** — a real claude-opus agent on a `--with-agent` install ran the full pull-loop (assign→wake→`start_work`→active→真做活[ran `ls -A`, described output]→complete); active-slot released to 0 (Tester2 report #3).
3. **Go-mechanism (deterministic)** — PR7 `TestPR7_SingleActiveByConstruction`: an agent with two queued WorkItems has its 2nd `start_work` rejected (`ErrAgentHasActiveWork`); the slot never exceeds 1.
4. **Deployed DB structural guarantee** — on a real install (SELECT-only, teardown), schema-version = 53 applied and the partial-unique index is enforced at the DB layer:
   ```sql
   CREATE UNIQUE INDEX idx_awi_agent_active
     ON agent_work_items (agent_id) WHERE status IN ('active','waiting_input')
   ```
   → even if application logic races, the DB rejects a 2nd active WorkItem. **By-construction, not by-convention.**

### 2.3 PR7 — D pull-model Go integration/e2e suite (6/6, Tester §19 lane)

`internal/projectmanager/service/d_pull_flow_test.go` (test-only, merged @ 91f3e17). Cross-BC harness (pm service + agent service via transition-sink + relay[3 projectors] + reconciler + conversation BC) on one shared in-mem DB. Six mechanism invariants end-to-end:

1. **HappyPullFlow** — assign→queued (task stays OPEN, assignee=metadata)→start_work→active+running+slot=1→complete→completed+WI done+slot freed.
2. **SingleActiveByConstruction** — 2nd start_work rejected; slot never >1 (the @oopslink bug).
3. **TaskWISync_FailedToBlock** — agent fail→WI failed→task Blocked+slot freed ((b) path).
4. **ReconcilerReleasesStuck** — NO-FALSE-KILL within window (Tick 0) / past staleAge→Tick 1→FailFromAgentDeath CAS→task Blocked.
5. **DualStream** — get_my_unread (DM-all + channel @mention only; non-mention chatter excluded) + mark_seen dedup.
6. **CompleteVsReleaseRace** — stale WI but agent completes first → reconciler sees done WI → releases 0, task stays completed (no spurious block).

### 2.4 Notable verify-not-trust catches that shaped the backend

- **State-machine simplify (A) is safe** — independently confirmed the task events are *already* generic (`EvtTaskStateChanged`, no per-action events) and dispatch hangs on `EvtTaskAssigned` (not status), so the single-generalized-endpoint design loses no events.
- **Free-model task↔WI decoupling is INTENDED, not a (b)-limbo bug** — under SetStatus, a task can be "running" with its WorkItem "done" (status = agent self-reported label, center doesn't enforce the WI); the WorkItemProjector `EvtTaskStateChanged` handler is graceful for any target (specific cases + default no-op). Distinct from the (b) WI-failed limbo, where the system-projector MUST converge task→Block. Both faces classify by this distinction.
- **Tags 16-char limit is RUNE-based** (`len([]rune(tag)) > 16`) = CJK-correct (a 16-Chinese-char tag = 48 bytes is accepted). Propagated cross-layer: the FE chip input now counts by code-point (`[...tag].length`), and a CJK boundary test was added to the backend.
- **Batch PATCH is atomic all-or-none** (single tx; one invalid field → whole rollback, no partial-apply, no version bump) — confirmed by `TestBatchUpdateTask_Atomic`.

### 2.5 Force-delete orphan-tolerance — joint run-real (Tester data ↔ Tester2 UI)

**CORE data-face GO:** `DELETE ?force=true` → 200 `{deleted:true}`; the agent row is gone but its message/participation soft-refs are PRESERVED (history kept). Opening the affected channel renders message content with **no crash, 0 page-error, 0 console-error** (clicking the deleted-agent sender does not crash). This is the (i) orphan-tolerated data semantic holding end-to-end on a real `--with-agent` install — the data-side (soft-ref rows remain, no FK-block, queries stay graceful) is exactly what Tester2's run-real UI observed (history preserved + no crash). Consistent with Dev's grounding (schema has zero FK → agents).

**Honest scope:** the data-face conclusion this round is established via Tester2's run-real UI behavior + the (i) grounding; her instance was torn down before a separate live SQL pass — the orphan SQL queries (agent deleted / WI swept / read-state+activity soft-ref rows remain) are documented for an independent confirmation run if an instance is available.

**verify-not-trust correction (Tester2):** the initial `deleted_label_present=true` was a FALSE-POSITIVE (seeded message body literally contained the string "(deleted)"); the UI does **not** actually render a friendly "(deleted)" label. → two minor UX findings, **non-blocking for v2.8.1 tag** (the (i) decision accepts soft-ref; this is polish), recommended as follow-ups (also in § 3):
- **FINDING 1 (minor UX):** a deleted agent's message sender renders as the raw identity-ref `agent:<id>` (not a friendly "(deleted)" or the prior display name).
- **FINDING 2 (minor UX):** clicking a deleted agent's sender → sidebar (`GET /api/agents/{id}` 404) resolves to **blank** (no friendly "deleted / unavailable" state).

### 2.6 Method/discipline notes
- Every GO is content-delta byte-identical to the merged trunk file (provably commit-bound, not just "the PR looked right").
- Full `go test ./...` run per PR (not changed-packages-only) — defense-in-depth ship-gate.
- Zero prod footprint: all real-install checks in `/tmp` or separate test namespace, SELECT-only on shared instances, teardown after.

_Pending final-append (after Dev2 Edit-Task modal FE ship): the Edit-Task modal CJK-tag + batch-atomic real-endpoint joint (my endpoint SQL ↔ Tester2 modal UI)._

---

## § 3 — FE UI Acceptance (Tester2)

_(Tester2 writes: reports #1/#2 dark/light × sidebar matrix + REV4 palette + 4-filter + chat bubbles + sender-sidebar + §3.3 GOs; plus the force-delete orphan UI findings 1 & 2.)_

---

## § 4 — Real-LLM Behavioral Acceptance (Tester2)

_(Tester2 writes: behavioral ① pull-loop real-claude PASS; ②③ dual-stream/stuck-release ack-PR7-covered + early run-real #206/#209.)_

---

## § 5 — Pending (final-append)

_(PD updates after Dev2 ship.) Edit-Task modal CJK-tag/batch-atomic UI ↔ Tester real-endpoint • 7th DM increments 3-5 • TaskDetail sidebar redesign._

---

## § 6 — Ship Decision + Tag

_(PD writes final.)_
