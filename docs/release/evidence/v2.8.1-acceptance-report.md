# v2.8.1 Acceptance Report

**Release:** v2.8.1 — #278 D pull-model rollout + @oopslink polish/feature set
**Trunk at draft:** v2.8.1 @ 89f3376
**Status:** PARTIAL (Phase 3 in progress — completed faces committed now; pending faces final-appended after Dev2 Edit-Task modal FE ship)

Acceptance is structured as a multi-face cross-check: Backend Data/API (Tester), FE UI (Tester2), Real-LLM Behavioral (Tester2), with PR-level §-1 sign-off (PD) per the team's independent-acceptance discipline (PD intent + Tester criteria, Dev zero design involvement).

---

## § 1 — Executive Summary

_(PD writes after all faces fold.)_

---

## § 2 — Backend Data/API Acceptance (Tester)

**Scope:** independent black-box + white-box data/API acceptance of the v2.8.1 backend (the #278 D pull-model rollout + the @oopslink polish/feature set), conducted by the Tester lane independent of Dev's design. **Method per PR:** build the PR independently → black-box behaviour + **full `go test ./...`** (defense-in-depth ship-gate) → white-box code-confirm of the invariant → **content-delta** the merged trunk file vs my-verified commit (byte-identical) so the GO is provably commit-bound. Pre-existing `workerdaemon`/`agentsupervisor` PTY/IO flakies classified as env-noise (not regressions), ruled out per finding.

### 2.1 Verdict summary — 11/11 backend verifies GO + landed + content-delta byte-identical to trunk

| # | Capability | PR | Landed @ | Verdict |
|---|---|---|---|---|
| 1 | A6 dual-stream (get_my_unread scope + mark_seen) | #206 | trunk | ✅ GO |
| 2 | A7 reconciler / 30-min watchdog (stuck→release, NO-FALSE-KILL, race-symmetric) | #209 | trunk | ✅ GO |
| 3 | A8 cutover (pure pull-model; start_work sole activation) | #212 | trunk | ✅ GO |
| 4 | (b) task↔WI sync (failed ANY cause → Task.Block) | #214 | trunk | ✅ GO |
| 5 | Version endpoint/panel | #217 | trunk | ✅ GO |
| 6 | State-machine model fix (assigned→metadata; canceled/withdrawn→discarded; migration 0052) | #221 | trunk | ✅ GO |
| 7 | Force-delete agent/worker (orphan-sweep + unbind) | #223 | 9c72061 | ✅ GO |
| 8 | Filter time-range (tz-safe, no off-by-one) | #224 | c10e3cc | ✅ GO |
| 8b | Filter 4-way combo (status∧project∧assignee∧date, AND-gate, OR-within-set) | (same endpoint) | c10e3cc | ✅ GO (code-confirmed) |
| 9 | Free state-machine SetStatus (any-valid, no adjacency, POST /status) | #229 | 0273650 | ✅ GO |
| 10 | Edit-Task (tags + status_changed_at + batch PATCH atomic) | #232 | 89f3376 | ✅ GO |
| 11 | PR7 — D pull-model Go integration suite (6/6) | #231 | 91f3e17 | ✅ MERGED |

### 2.2 Headline: @oopslink's "multiple agents running simultaneously" bug — eradicated, FOUR-FOLD坐实

The single-active invariant (one agent ⇒ at most one active WorkItem) is proven across four independent layers:

1. **Code race-gate** — PR1 race-gate + migration 0051 single-active index.
2. **Real-LLM behavioral** — a real claude-opus agent on a `--with-agent` install ran the full pull-loop (assign→wake→`start_work`→active→真做活[ran `ls -A`, described output]→complete); active-slot released to 0 (Tester2 report #3).
3. **Go-mechanism (deterministic)** — PR7 `TestPR7_SingleActiveByConstruction`: an agent with two queued WorkItems has its 2nd `start_work` rejected (`ErrAgentHasActiveWork`); the slot never exceeds 1.
4. **Deployed DB structural guarantee** — on a real install (SELECT-only, teardown), schema-version = 53 applied and the partial-unique index is enforced at the DB layer:
   ```sql
   CREATE UNIQUE INDEX idx_awi_agent_active
     ON agent_work_items (agent_id) WHERE status IN ('active','waiting_input')
   ```
   → even if application logic races, the DB rejects a 2nd active WorkItem. **By-construction, not by-convention.**

### 2.3 PR7 — D pull-model Go integration/e2e suite (6/6, Tester §19 lane)

`internal/projectmanager/service/d_pull_flow_test.go` (test-only, merged @ 91f3e17). Cross-BC harness (pm service + agent service via transition-sink + relay[3 projectors] + reconciler + conversation BC) on one shared in-mem DB. Six mechanism invariants end-to-end:

1. **HappyPullFlow** — assign→queued (task stays OPEN, assignee=metadata)→start_work→active+running+slot=1→complete→completed+WI done+slot freed.
2. **SingleActiveByConstruction** — 2nd start_work rejected; slot never >1 (the @oopslink bug).
3. **TaskWISync_FailedToBlock** — agent fail→WI failed→task Blocked+slot freed ((b) path).
4. **ReconcilerReleasesStuck** — NO-FALSE-KILL within window (Tick 0) / past staleAge→Tick 1→FailFromAgentDeath CAS→task Blocked.
5. **DualStream** — get_my_unread (DM-all + channel @mention only; non-mention chatter excluded) + mark_seen dedup.
6. **CompleteVsReleaseRace** — stale WI but agent completes first → reconciler sees done WI → releases 0, task stays completed (no spurious block).

### 2.4 Notable verify-not-trust catches that shaped the backend

- **State-machine simplify (A) is safe** — independently confirmed the task events are *already* generic (`EvtTaskStateChanged`, no per-action events) and dispatch hangs on `EvtTaskAssigned` (not status), so the single-generalized-endpoint design loses no events.
- **Free-model task↔WI decoupling is INTENDED, not a (b)-limbo bug** — under SetStatus, a task can be "running" with its WorkItem "done" (status = agent self-reported label, center doesn't enforce the WI); the WorkItemProjector `EvtTaskStateChanged` handler is graceful for any target (specific cases + default no-op). Distinct from the (b) WI-failed limbo, where the system-projector MUST converge task→Block. Both faces classify by this distinction.
- **Tags 16-char limit is RUNE-based** (`len([]rune(tag)) > 16`) = CJK-correct (a 16-Chinese-char tag = 48 bytes is accepted). Propagated cross-layer: the FE chip input now counts by code-point (`[...tag].length`), and a CJK boundary test was added to the backend.
- **Batch PATCH is atomic all-or-none** (single tx; one invalid field → whole rollback, no partial-apply, no version bump) — confirmed by `TestBatchUpdateTask_Atomic`.

### 2.5 Force-delete orphan-tolerance — joint run-real (Tester data ↔ Tester2 UI)

**CORE data-face GO:** `DELETE ?force=true` → 200 `{deleted:true}`; the agent row is gone but its message/participation soft-refs are PRESERVED (history kept). Opening the affected channel renders message content with **no crash, 0 page-error, 0 console-error** (clicking the deleted-agent sender does not crash). This is the (i) orphan-tolerated data semantic holding end-to-end on a real `--with-agent` install — the data-side (soft-ref rows remain, no FK-block, queries stay graceful) is exactly what Tester2's run-real UI observed (history preserved + no crash). Consistent with Dev's grounding (schema has zero FK → agents).

**Honest scope:** the data-face conclusion this round is established via Tester2's run-real UI behavior + the (i) grounding; her instance was torn down before a separate live SQL pass — the orphan SQL queries (agent deleted / WI swept / read-state+activity soft-ref rows remain) are documented for an independent confirmation run if an instance is available.

**verify-not-trust correction (Tester2):** the initial `deleted_label_present=true` was a FALSE-POSITIVE (seeded message body literally contained the string "(deleted)"); the UI does **not** actually render a friendly "(deleted)" label. → two minor UX findings, **non-blocking for v2.8.1 tag** (the (i) decision accepts soft-ref; this is polish), recommended as follow-ups (also in § 3):
- **FINDING 1 (minor UX):** a deleted agent's message sender renders as the raw identity-ref `agent:<id>` (not a friendly "(deleted)" or the prior display name).
- **FINDING 2 (minor UX):** clicking a deleted agent's sender → sidebar (`GET /api/agents/{id}` 404) resolves to **blank** (no friendly "deleted / unavailable" state).

### 2.6 Method/discipline notes
- Every GO is content-delta byte-identical to the merged trunk file (provably commit-bound, not just "the PR looked right").
- Full `go test ./...` run per PR (not changed-packages-only) — defense-in-depth ship-gate.
- Zero prod footprint: all real-install checks in `/tmp` or separate test namespace, SELECT-only on shared instances, teardown after.

### 2.7 Edit-Task modal — real-endpoint joint (Tester data ↔ Tester2 modal UI)

**GO.** The Edit-Task modal (PR #235, merged to trunk) drives the #232 backend end-to-end, verified same-source:

- **CJK-tag rune-16** — backend `len([]rune(tag)) > 16` + the CJK boundary test (16 中文 accepted / 17 rejected, content-delta byte-identical on trunk) ↔ Tester2 FE computed-truth (16-CJK [48 bytes] accepted; 17-CJK / 17-ASCII rejected via in-modal validate using `[...tag].length`). Rune-correct on both sides — the cross-layer catch closed.
- **batch-atomic + dirty-only** — backend `BatchUpdateTask` is pointer-based (`nil`=unchanged, non-nil=applied; single-tx all-or-none, `TestBatchUpdateTask_Atomic` asserts rollback with no version bump) ↔ Tester2 FE request capture (editing only the title → PATCH body = `{"title":"..."}` dirty-only; a 400 leaves the modal open showing the error). Dirty-only pointer semantics consistent both sides.
- **contract key** — `description` (not `desc`); verify-not-trust re-confirmed against the handler body struct, matches the FE.
- **status_changed_at** — set on status change, not on metadata edits (backend unit-verified).

**Honest scope:** the data-side this round is established via backend unit tests (content-delta byte-identical to trunk) + Tester2's run-real FE integration (the modal→endpoint contract); no separate independent live SQL pass was run this round (the FE instance was torn down). The data-side behaviour is unit-verified and the integration FE-confirmed — a sufficient same-source for this item.

= **§ 2 backend/data face COMPLETE.** All 11 backend verifies + PR7 (6/6) + the two run-real joints (force-delete orphan, Edit-Task real-endpoint) are GO.

---

## § 3 — FE UI Acceptance (Tester2)

**Methodology:** real-instance run-real (isolated ns, prod-zero-footprint, teardown after) · computed-truth (`getComputedStyle` rgb / `.value` / DOM) · both-mode (light+dark each computed) · verify-not-trust (disprove before flagging) · design-time pre-verify (cheaper than run-real). Screenshots: `docs/release/evidence/screenshots/`.

### 3.1 Dark/light × sidebar collapse/expand — screenshot matrix (@oopslink 重点) — ✅ GO
Real instance trunk `0273650`. 4-cell matrix (`{light,dark}_{expanded,collapsed}.png`):
- **theme-toggle**: light body-lum 250 ↔ dark 26.7 (html.dark applied; full chrome — sidebar+main+chips — both modes correct).
- **sidebar-collapse-toggle**: expanded 208px (icon+labels) ↔ collapsed 56px (icons-only + expand chevron); content area reflows. 0 console error.

### 3.2 Status palette REV4 (white-on-color, 8 colors) — ✅ GO
@oopslink iterated **4 palette revisions**; my design-time pre-verify shaped each (caught zinc≈slate 5.1 / stone-still-6.4 / yellow-700↔rust 24.3 clash; confirmed REV4 clean before build). Final REV4 run-real (trunk `bef2cb7`), 8/8 colors render == locked + white text: open=sky `#0284c7` (2,132,199) · in_progress/running=blue `#2563eb` · **blocked=blockedred `#dc2626` (custom token, not `bg-red-*`, guardrail-safe)** · resolved/completed=green `#16a34a` · verified=teal `#0d9488` · closed=slate `#64748b` · discarded=zinc `#3f3f46` · reopened=amber `#d97706`. Distinctness rendered min 64.4 (green-vs-teal). White-on-color marginal AA (sky 4.1/green 3.3/teal 3.74/amber 3.19, large-OK) **@oopslink-accepted** (视觉一致 > strict AA, documented).

### 3.3 4-filter (status/project/assignee/date-range) cross-tz — ✅ GO
Real instance trunk `0a26316`, Playwright `timezoneId` GMT+8:
- **tz-命门 (verify-not-trust):** select "today 6/8" → request `created_after=2026-06-08T00:00:00+08:00` (local offset, **NOT Z, NOT naked date**) → no off-by-one (joint flag with Tester closed run-real).
- filter-actually-filters: date→3 rows; **compound AND**: date+assignee→1 row (correct intersection); clear-all→full reset; assignee **kind-cue not-color-only** ("name · user").

### 3.4 Chat UX redesign (PR #230 bubbles + #233 sender-sidebar) — ✅ GO
Real instance trunk `89f3376` (`chat_{light,dark}.png`):
- **#230:** unified bubbles — own = indigo-500 `#6366f1` (99,102,241) right / agent+user = bg-subtle left (own-detection correct via identity-ref compare); name-above/timestamp-below (iMessage-style); max-w adaptive; avatar indigo-only gradient (indigo-400 excluded for AA).
- **#233:** sender name/avatar = keyboard-accessible **button** → SenderDetailSidebar slide-in (kind-dispatch agent/user) + Esc-close + overlay-close + useModalA11y (focus-trap/restore/aria-modal, PR #210 reuse) + ASCII-X (not emoji) + motion-reduce.
- indigo-500 white = 4.47 marginal-AA (chat text-sm ~14px regular technically <4.5; @oopslink-accepted; large-text precision noted: WCAG large = ≥18.66px/14px-bold).

### 3.5 Incremental §3.3 GOs (this session, all real-instance prod-zero-footprint)
version-panel #216/#217 (panel == real build, branch/built_at ≠ "unknown") · de-emphasis #218 (raw error hidden→[Details]) · state-machine #220 (no-'assigned'-leakage class-sweep; "assigned"="ASSIGNED TO" false-positive caught) · force-delete #222/#223 (typed-name disabled-until-exact) · chat right-align #225 (brand-hover both-AA) · FilterBar #208 (found+drove Esc-close fix→3-modal sweep #210) · chip dark-contrast #204/#205 (found #228 dark-AA bug) · 5th 2-col #207.

### 3.6 force-delete orphan-tolerance UI — findings (see also §2.5)
**🟡 F1:** deleted agent's message sender renders raw identity-ref `agent:<id>` (not friendly "(deleted)"/former-name) — graceful (no crash) but raw. **🟡 F2:** sender-detail sidebar (#233) for deleted agent (GET `/api/agents/{id}` 404) resolves to **blank** (no "(deleted)/unavailable" message). Both FE-polish, **non-blocking for tag** (soft-ref accepted per (i)), routed to **v2.9** deleted-peer-UX follow (Dev2 owns: F1 displayName resolver, F2 sidebar 404 handling).

### 3.7 Edit Task modal (PR #235, the final-append item) — ✅ GO
Real instance (branch `dev2/v281-edit-task-modal` on the merged #232 backend). Full batch-edit form: title / description / status (free SetStatus) / assignee (useMembers) / tags chip + submit/cancel/close, opens via `task-edit-button`, useModalA11y (Esc-close).
- **🎯 CJK rune-16 validation (verify-not-trust core, matches backend `len([]rune) > 16`):** a 16-Chinese-char tag (16 runes = **48 bytes**) is **ACCEPTED** (counted by rune, not byte — would wrongly reject at 48 bytes); 17-CJK and 17-ASCII tags are **REJECTED** ("Tag too long (max 16)"). Confirms `[...tag].length` rune-correct (my design-time flag, applied by Dev2).
- **🎯 Batch dirty-only save:** changing only the title → **one** `PATCH /tasks/{id}` with body `{"title":"…"}` (only the dirty field, not status/assignee/tags/description) → 200 → modal closes. Atomic dirty-only UI ↔ Tester real-endpoint (POST/PATCH atomic, one-field-error→400-rollback-stays-open).
- max-10 tags + trim/empty-reject/dedup + in-modal validate (pre-empts the atomic 400). 0 console errors.

> **§3.4 note:** the #233 chat-UX-1 verify (in this report) was run on a trunk that, at the time, carried 2 hidden test-failures from #233's `✕`→ASCII-X change (stale test assertion + a literal-`✕` comment tripping the no-emoji guardrail). Dev2 fixed both in PR #235 → full suite 578/578 green. The #233 UI behavior I verified (rendered correctly) was unaffected; the test-fail was assertion/comment-level. Chat UX 2 (#D1E3FF) will supersede §3.4's indigo bubble when shipped.

---

## § 4 — Real-LLM Behavioral Acceptance (Tester2)

### 4.1 ① Pull-loop (D 灵魂) — real claude-opus agent — ✅ GO
Real `--with-agent` instance trunk `89f3376` (real claude-opus + worker). Dispatch task → real agent **wake → pull (`start_work`) → active → did real work → complete**: WI queued→active→done (v1→v2→v3, ~17s), task=completed (completed_by=agent), active-slot freed=0.
- **verify-not-trust:** agent posted real output _"Ran `ls -A` in the repo root. The only entry is `.claude`… No other files present"_ = real LLM work, **NOT a status flip**.
- = @oopslink "agent decides, center stores" pull-model + single-active by-construction, end-to-end with a REAL agent. Complements Tester PR7 Go-mechanism HappyPullFlow (mechanism + real-LLM both confirmed).

### 4.2 Single-active root-fix — FOUR-fold confirmation (headline)
@oopslink "多 agent 同时跑" bug root-eradicated, confirmed 4 independent ways:
1. **Code race-gate** (PR1 + migration 0051 index)
2. **Real-LLM behavioral** (§4.1 — real agent, slot freed to 0)
3. **Go-mechanism** (Tester PR7 SingleActiveByConstruction, deterministic 6/6)
4. **Deployed UNIQUE index** (real install `sqlite_master`): `CREATE UNIQUE INDEX idx_awi_agent_active ON agent_work_items (agent_id) WHERE status IN ('active','waiting_input')` — DB-level by-construction; even if app logic races, the DB rejects a 2nd active WI. schema-version=53 applied.

### 4.3 force-delete orphan-tolerance — real-LLM/UI face (see §2.5 + §3.6)
Real `--with-agent` instance. **CORE PASS:** force-delete agent → agent gone, messages soft-ref preserved, channel renders, **no crash / 0 page-error / 0 console-error** (incl. clicking deleted-agent sender) = deleted-peer core HOLDS. 2 minor UX findings (F1 raw-ref sender, F2 blank sidebar) → v2.9 (§3.6).

### 4.4 ② dual-stream — run-real ✅ GO ; ③ stuck-release — PR7-covered
- **② dual-stream — run-real confirmed THIS round** (real `--with-agent` instance, trunk 89f3376): sent the agent a directed DM ("what is the capital of France? One word") → the real claude agent **saw it via get_my_unread (didn't miss the directed message) and replied "Paris" = NON-HOLLOW** (a real answer, not an ack/status). Also confirmed earlier in PR #206 run-real (`post_message` reply); mechanism = Tester **PR7 DualStream** (get_my_unread scope + mark_seen dedup).
- **③ stuck-release — PR7-covered + early run-real:** NO-FALSE-KILL confirmed in **PR #209 run-real**; true-kill = Tester **PR7 ReconcilerReleasesStuck** (FakeClock, deterministic). Real-time stuck hard to orchestrate (real agent completes fast).
- = **behavioral matrix: ① pull-loop + ② dual-stream both run-real (real claude agent); ③ stuck-release PR7-mechanism + #209 run-real.** Mechanism (PR7) + real-LLM both confirmed across the D 灵魂.

---

## § 5 — Pending (final-append)

**✅ Edit-Task modal — DONE** (§3.7 §3.3 GO + Tester real-endpoint joint). The last tag-blocker FE acceptance item is closed.

_Still pending (post-ship, do not block tag — design evolution / polish):_
- **Chat UX 2** (#D1E3FF light-blue + dark text [13.72 AAA, supersedes #230 indigo] · name/time outside bubble · `formatChatTime` ISO+sec+tz chat-only) — Dev2 next batch → §3.4 update.
- **7th DM increments 3-5** (3-zone DM layout + composer + attachment cards).
- **TaskDetail 2-section sidebar redesign** + status-duration "Xh Ym".
- **v2.9 follow:** force-delete deleted-peer UX (F1 displayName resolver / F2 sidebar 404), §3.6.

---

## § 6 — Ship Decision + Tag

_(PD writes final.)_
